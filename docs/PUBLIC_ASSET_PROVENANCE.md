# Public candidate asset provenance

2026-09-06. Records source provenance, not an open-source license grant, trademark
clearance, comprehensive asset audit or publication approval.

## public/favicon.svg

Replaced locally with an original code-authored geometric mark: a rounded square,
three status dots and three horizontal bars. Authored in this project with AI assistance
using SVG primitives; no imported image, font, icon library, tracing or external resource.
The palette matches the standalone welcome-page draft. The asset contains no script,
links, embedded image or dependency.

SHA-256: `b718117c09caff435de086a545f5c27c8b22497a1be2b3069f595ccad3da268a`.
SVG XML validation passed. The standalone VPS build copied identical bytes to its
required client-asset path; asset loader tests and inventory diagnostics passed.

The prior blue four-part icon entered the initial application import. Its external
source/rights were not established. That historical version remains in private Git
history but is no longer the current candidate asset. Do not export private history or
reuse the old icon from historical inventory snapshots as if this note cleared it.

The owner's original-code/asset license choice and branding acceptance remain pending.
The planning inventory must retain this file as a required reviewed asset, not silently
omit it or describe it as cleared for distribution. Other assets, including application
preview images, require their own review; this entry does not clear them.

The already transferred private welcome website contains no favicon and is unchanged.
