# Root review: remaining per-path checker alternative

2026-09-08; baseline1449c64. Read actual per-path fixture and retained exit0 receipt;
no second install or runtime invocation by reviewer. This review is independent of
the fixture author, not independent package acquisition.

The comparison addresses the previous unresolved alternative rather than repeating
the already-failed whole-application traversal. Actual checker4.4.2 public init runs
against current type-fest5.9.0 at the recorded physical package path and owned small
fixtures. Existing manifest/lock/type-fest notice hashes are checked. The actual
type-fest output identifies one licenseFile despite two original license files.
The dual-license fixture demonstrates one selected license, a separate NOTICE path,
and licenseText normalization rather than preservation of original whitespace/quotes.
The retained clarification input can preserve explicit supplied text. Its previous
checksum behavior remains separate existing evidence, not newly exercised here.

These are interface capabilities, not allegations of legal noncompliance. An OR
license may allow choosing an alternative; the comparison's responsibility is our
version-bound original-text evidence and attribution assembly, not deciding which
license rights a distributor must elect. Original-byte preservation and multiple
attachments avoid another custom reconstruction layer for this responsibility.

Source inspection and executed results therefore discriminate the contenders:
checker remains useful for its supported single-text/clarification workflow, but
using it here would still require additional discovery/extraction or explicitly
feeding known original texts. The actual public CycloneDX gatherer already performs
that role across the current39-identity corpus and named exceptions. Keep pnpm for
graph identity and the tested bundle plugin for module scope.

No blocking defect in the narrow evidence. Do not claim all checker options/versions
or legal use cases are rejected, use fixture duration as a performance ranking, or
infer whole-release licensing clearance. Final release scope, nested notices, copied
source and build-dependent assets still require separate reconciliation.
