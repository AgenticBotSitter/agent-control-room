# Independent RC6 update-primitives review

2026-09-08. Source/receipt review only; no tests, downloads, native services or database operations executed by reviewer. Reviewed f6-update-fit.md, condensed evidence, authored fixture, matching existing launcher/connector tests, actual native journal constructor, launcher and service diagnostics.

**No blocking finding within the report's limited primitive scope. RC6 is not accepted as a completed updater or continuous service.**

- Count reconciles: authored file contains2 test cases. Independently matching the exact recorded name-pattern against the two existing test sources selects12 (6 launcher,6 connector), total14. Existing tests contain more cases, so this is not a complete suite pass. The saved receipt explicitly says condensed transcription, not raw stdout. Reviewer can reconcile counts and assertions, not independently attest original terminal execution or timings from that transcription.
- Actual journal fidelity is sound. Children import the actual journal module, use distinct cwd values but identical implementation path, and reopen one database outside both directories. Reservation/state and file-hash assertions establish persistence/no renewed reservation, not a cross-version update or live release switch. Schema test really changes SQLite user_version then invokes the real constructor and checks unchanged file hash. Constructor rejects version2 and closes its DB handle on error. The report does not overclaim broader rollback compatibility.
- Launcher/connector distinctions are accurate. Actual runPrivateNode uses injected release/operator/runtime; SIGTERM is emitted from EventEmitter. Connector tests use synthetic host/runtime methods and selected mock-timer cases. Native lifecycle, remote cleanup, OS supervisor and fleet scheduling are not exercised. providerCalls0 in new child output is a literal inert-fixture label, already disclosed as such; it is not a measurement.
- Cleanup is bounded in source: owned mkdtemp roots, t.after removal and explicit absence assertions for the two new fixtures; child spawnSync has15second timeout and output cap. Existing launcher fixtures remove their own roots via hooks. No independently retained root-path inventory or raw process/cleanup receipt exists in the condensed evidence, so reviewer does not separately certify host-wide absence or descendant containment. No such claim is needed for the stated ordinary local fixture result.
- Missing-system claims match inspected entrypoints: fixed compiled launcher target, explicit initial/recover one-shot, no generic version selector; diagnostics explicitly returns continuous_service_not_available. The report correctly leaves service templates noninstallable and Windows/Linux/macOS target qualification separate.
- Reuse recommendation avoids inventing a duplicate store or supervisor. Reuse current journals, close/recover and native supervisor lifetime; evaluate existing updater patterns before designing custom coordination. Future compatibility/drain/readiness acceptance is proposed work for root design, not proof these implementations exist or every deployment must adopt a particular updater product.

Evidence accepted only as source-backed, author-reported local primitive checks. Keep RC6 continuous pickup/drain, actual versioned update/recovery and native host qualification open. When the future real updater is exercised, retain direct machine-readable output and exact owned-root cleanup receipts rather than relying solely on condensed transcription. No rerun requested for this report-only review.
