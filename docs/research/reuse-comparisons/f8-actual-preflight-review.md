# Root review: actual candidate preflight

Reviewed complete wrapper source, original fixtures, report and JSON receipt. The
receipt records exit0 and13 cache,3 cached-trust verification and7 rehearsal cases
per candidate:46 scenario instances. Source hash guards and candidate injection
preserve the original assertions. Actual token-library verification executes, while
invalid-key factory checks remain CR policy. No blocking finding for this scope.

This is not a claim of actual database access: the valid path deliberately throws
at its open sentinel before creating a pool. It is also not complete async safety,
all-route integration or owner authentication. Root did not rerun the candidate
fixture; the author retains execution evidence and root independently reviewed it.

The acquisition ledger uses `ownedRoot`, not `root`. Root's first generic inspection
looked up the wrong field and did not establish cleanup. After reading the actual
ledger, root separately ran `test ! -e /private/tmp/cr-f8-jwt.8b8w0O`; exit0 confirms
absence of the exact owned cohort. No additional deletion or rerun was needed.

Accepted as scoped comparison evidence only. The actual cache/rehearsal candidate
execution gap is closed for these matrices. Do not rerun them unchanged; finish
remaining async decision/cost and production implementation regression scope.
