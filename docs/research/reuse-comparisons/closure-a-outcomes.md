# Independent closure reconciliation: A1–A7

2026-09-08. Basebc0a61a plus current research; read-only reconciliation, not new experiment, implementation authority or final selection. Covers the original seven A outcomes without dropping their cross-family dependencies. Sources: comparison program/closure/implementation plan, mapped dossiers and independent reviews below. No application files changed. No new resource or maintenance measurements are claimed.

## A1 — Private website and persistent database

**Settled responsibility choices.** Keep PostgreSQL as transactional authority and the existing compiled website/operator/migration interfaces. DR-10 conditionally selects node-postgres at the bounded driver port; the comparison is closed, not its implementation. Keep native logical `pg_dump`/`pg_restore` for a dedicated database. pgBackRest is a complementary physical/WAL/cluster-recovery option, not a competing implementation of the selected dedicated-database archive procedure. Use the actual host supervisor; do not introduce a new supervisor platform. `f6-supervisor-fit.md` and `f6-update-fit.md` establish source/configuration dispositions and14 existing journal/close primitives, not an installer or updater.

**Remaining local comparison.** RC9 still needs one actual native PG17 dump/restore across representative current-schema data, ownership/ACL/required-row/restricted-login checks and the separately restored artifact references. Use the existing procedure/preflight, not a fake SQL exporter or another tiny-marker-only pass. `f8-pg17-inspection.md` verifies the acquired package/signature/tool inventory only: it expressly leaves recursive dependencies/runtime and restore execution open. Those are local prerequisites/evidence, not owner inputs. Root is handling this; do not rerun acquisition or claim restore success from it. No additional pgBackRest service test is required merely to select native logical restore; full physical recovery remains its own explicitly scoped future responsibility.

**Implementation versus target.** Implement driver/caller parity, immutable package/config/state separation, reviewed supervision and backup/restore commands locally. Production dedicated DB/roles, verified persistent mount/container replacement, actual supervisor namespace and restored restricted-login acceptance are live gates. Do not alter the shared primary or other applications to obtain them.

**Batch destination.** First usable task, with backup work started early; complete operational recovery in Daily-use release. Keep `deploy/BACKUP_RESTORE.md`, `deploy/SUPERVISION.md`, private preflight and bounded database authority. Retire Postgres.js only after the selected driver packet migrates all consumers; zero production lines removed now. No new resident service for logical backup; current distribution/transitive notices and resource budget still need release acceptance.

## A2 — Login, MFA and owner bootstrap

**Settled.** Retain Cloudflare Access/provider and canonical subject/policy. DR-05 selects jsonwebtoken9.0.3 behind the synchronous verifier after actual policy/caller/cache/bootstrap/rehearsal comparison against jose6.2.12 and retained current crypto. `f8-jwt-selection.md` supplies scoped scores, MIT scope, pin, exact evidence and integration cost. Authelia is source-screened as an optional self-hosted identity deployment, not needed to fix a currently uncovered owner requirement.

**Remaining local comparison: none for this selection.** Do not repeat JWT contests or require the entire login subsystem before treating the representative interface choice as settled. Typed package import, downstream regressions, preserved freshness/identity policy and notices/build are implementation acceptance, not missing candidate research.

**Target/batch.** First usable task/IP-01. Actual owner subject/MFA, denied other owner, logout/expiry/revocation/origin and live routing remain target acceptance. Preserve approved authentication restrictions on rollback; no bypass. Replace only standard token processing, not policy, consent or key discovery. Dependency cost is documented; resource comparison remains unknown rather than zero.

## A3 — Owner execution approval and local key custody

**Settled scopes.** Retain canonical preparation, consent/packet identity, paired issuer and review controller. E55–E58 and `f5-checkpoint-comparison.md` establish actual ssh2 1.17.0 signing framing/intake with disposable material and wrong-key/changed-content/late uncertainty checks. SSH agent signing is not SSH remote execution. Login/node authentication still do not authorize effects.

**Finite local closure gap.** The existing F5 dossier explicitly leaves actual OpenSSHAgent socket/custody and applicable platform alternatives uncompleted. Finish a source/interface disposition of the supported dedicated OpenSSH-agent-held key route versus the already-named platform custody option for each intended host context. If the actual ssh2 OpenSSHAgent-to-current signing port, unlike AgentProtocol bytes alone, is decision-changing, exercise that one socket/close/consent seam with disposable keys and synthetic review delivery. Do not redo E55–E58 signatures. Do not demand a live personal key to finish this local comparison or declare every platform custody mechanism equivalent from protocol framing. If a platform interface cannot provide the required signing contract, document that specific source exclusion; if it can and changes the choice, retain a bounded representative interface test, with unavailable host runtime marked as such.

**Implementation/target/batch.** First usable task on one chosen host, then Productive fleet host packets. Trusted review-input delivery, exact key/consent binding and an installed signer adapter are implementation work. Real dedicated owner key provisioning, attended consent/credential-store access and recovery custody are separate E4 permissions. ssh2's MIT framing scope does not clear whole platform packaging; release notices and per-platform native prerequisites remain. Preserve pending approval identities and uncertain attempts; do not invent a new consent protocol or auto-retry to make custody appear complete.

## A4 — Completion integrity across rollback

**Settled requirements, open store choice.** Preserve `AwaitableRollbackCheckpointStoreV1`, scope/digest/next-revision checks, bounded calls and staged SQL authority; use one independent anchor, not hot fallback. Actual etcd3.7.1 service plus actual CR adapter evidence exists; OpenBao2.6.2 scoped CAS/restart/unseal/deletion-denial service evidence exists. Reuse it. etcd READWRITE also deletes; OpenBao read/update has narrower observed deletion/recreation permissions, but added policy/unseal operations. Root Apache versus MPL separate-service scope is documented, not full distribution clearance.

**Exact decision-changing local work.** Resolve OpenBao's legitimate object/instance binding against the current contract before testing its actual canonical checkpoint adapter. Its version-created timestamp is not stable object identity (`f5-openbao-identity-map.md`); do not fake etcd responses or broaden authority silently. Then common finite cases: exact payload/scope advancement through actual completion interface, stale/concurrent contention where not already equivalent, lost successful response without repeated write, missing/replaced head, supported snapshot restore under pinned identity, and existing staged SQL/anchor split-commit recovery. Existing etcd callback-loss/restart observations are reusable; physical network-loss claims are unnecessary substitutes. Raw CAS and service restarts must not be repeated as though the adapter gap were untouched.

**Implementation/target/batch.** Root binding/choice followed by First usable task integrity integration; operational restore/recovery in Daily-use release. Real independent placement, owner-controlled restore custody and disaster-domain testing remain E4. They do not excuse the missing local adapter/restore/split-state comparison. Keep both candidates viable until discriminated; do not require a general secrets platform from OpenBao or custom consensus from Control Room. Remove only selected transport-specific adapter after parity; keep checkpoint schema/SQL gates. Resource receipts have distinct server workloads and cannot support a fabricated cross-service score.

## A5 — Task → real result → review → linked revision

**Settled pieces.** Keep canonical task/attempt/result/review authority and the existing ordinary journey; retain official Hermes interfaces and one-task launcher as useful execution components, not continuous fleet. DR-10 driver and DR-09 calendar primitives are settled. Actual pg-boss and DBOS transaction/worker cases now exist; typed/pg submission evidence already covers rollback, persisted synthetic disposition, collision and malformed delivery. Herdr observation is complementary, not an execution authority replacement.

**Remaining local decisions.** RC1: compare the existing actual queue delivery plus task-quality reconciliation phase through a representative synthetic task, independent eligible work during review, and bounded restart/uncertain-start/occurrence handling. DBOS durability and viable Hatchet post-commit/outbox/idempotency route need the corresponding decision-changing boundary, not a complete replacement engine. Do not rerun SQL markers, the same Promise-hold tests or successful driver probes. RC2: finish exact-identity lifecycle/recovery choice. TS exec lacks the native turn ID required by the observer; Python sync/async facade does not remove the demonstrated registration gap; current direct transport safely refuses same-turn early events but is not successful recovery. `f2-async-recovery-source.md` names supported exact-thread `thread/read(includeTurns=true)` as the bounded next discriminator, with absent/wrong/active/completed turn and unresolved usage treated honestly. No synthetic native IDs or reconstructed usage.

Existing joined ordinary result/review/child-revision evidence need not be recreated. Reconcile only changed candidate substitutions and missing restart/uncertainty with those real services. Full replacement implementation is not a prerequisite to conditional selection; a successful isolated SDK parser also is not enough to choose the complete client port.

**Implementation/target/batch.** First usable task joins one host/runtime; Productive fleet supplies continuous pickup across eligible hosts. Actual owner-authorized native result, profile/process ownership and provider recovery are E4. Preserve journals/IDs/uncertainty and link revision to retained result. The F1/F2 dossiers carry exact pins and license distinctions; candidate packaging/native binaries must become explicit runtime dependencies, never accidental research imports. Count adapter/state migration cost and rollback, not demo throughput.

## A6 — Project pages/tabs/archive/reopen

**Enough representative comparison for a conditional presentation choice.** Retain current canonical project catalog/lifecycle/pages. `f3-mounted-fit.md` already mounted actual current UI, Desktop ActiveSessionsBar and WebUI action/status code across actual protected project data, duplicate host/session IDs, offline/reconnect and delayed expiry. Seventeen corrected narrow checks supersede earlier cache-race evidence. Closing views did not cancel work; unmodified Desktop tab focus failure is an adaptation finding, not whole-product rejection.

**No need for another full dashboard contest.** Root can now choose retain-current plus narrowly adapted tab/action presentation, recording Desktop versus WebUI glue cost and keeping project identities. Native desktop/global-session dependencies do not justify copying entire shells. If the final choice is the minimal existing React page, basic missing keyboard/mobile/archive/reload behavior is implementation acceptance, not a requirement to build every contender's whole dashboard before selection. Do not omit those user requirements from the packet or label them already passed by JSDOM.

**Batch/target.** Essential project navigation in First usable task; full conversation/detail polish in Idea Lab/ABS. Actual browser layout, mobile/keyboard/reload can be locally accepted using disposable data during implementation; only live account/native session registration needs E4. Scope licenses to adapted source/dependencies, record changed files and avoid permission actions derived from prose. Development combined-bundle size is not comparative production cost; removed production authority lines remain zero.

## A7 — Results, review and Needs Me

**Settled authority.** Retain current protected bytes/digest/result/revision and record-derived attention reasons. `f3-journey-fit.md` crosses actual client/result components, while Desktop's assistant-text Approve/Deny branch is an unsuitable unchanged authority UI. Remove that action path if reusing presentation; no-op approval controls do not fix semantics. Actual Desktop Markdown/media closure20 checks and WebUI selected renderer's retained failures now exist; do not describe either as untested renderer source only.

**Small remaining presentation discriminator.** Compare the strongest lower-cost alternative—actual react-markdown/GFM directly inside the existing verified-result presentation, with selected Desktop code/diff reuse—against already-tested native-oriented renderer adaptation. Exercise representative protected result bytes, message-scoped state and retained-file link/navigation mapping, not a complete new transcript/product. WebUI mode/source/highlight inconsistencies belong to that selected composition, not a global product defect. Its actual E2 failures and extra host/global-state work can support a bounded disposition; no endless parser repair contest is required.

**Other linked gaps without duplication.** For final-response text, preserve current result receipt. Hermes session download E2 supplies selected bytes, not authenticated route/immutable run provenance; additional file attachments need the existing canonical binding plus a distinct reviewed intake/storage seam (`f2-hermes-intake-disposition.md`). Do not require an upstream signed manifest as a universal new requirement. Internal readiness is not an existing health route; its narrow protected read-only projection is implementation work, while RC9 daemon persistence/alerts and Beszel host-metric candidate comparison remain D2 responsibilities. A7 must link that work, not make all monitoring tools prerequisites for choosing result presentation.

**Batch/target.** Essential actionable missing/late/held/reviewable result path in First usable task; rich transcript/source files in Idea Lab/ABS; outage/monitoring in Daily-use release. Actual project→Needs Me→task→verified result/review navigation, identity revocation and file permissions are local implementation acceptance. Real retained provider output and live owner/mobile behavior remain E4. Never treat green queue/terminal status as reviewed completion; retain old result/download records through reversible presentation rollout.

## Finite consolidation corrections and nine-field gate

1. Remove stale plan paragraphs saying driver binding/shutdown/call-site selection remains open: DR-10 and the reviewed release/uncast cases supersede them. Lifecycle race/caller coverage is implementation acceptance, not permission to rerun the driver competition.
2. RC3's older Markdown-stub limitation is superseded by actual renderer dossiers; its remaining inexpensive direct-library composition is specific, not a generic “complete rendering” investigation. A6's representative mounted comparison is already enough for conditional UI direction.
3. RC6's lack of a continuous service/updater is honest implementation work; source dispositions and journal primitives must not be repeated until an entire service exists. Real OS service registration remains platform E4.
4. A3 source/custody mapping, A4 common adapter/restore/split-state, A5 engine/native recovery, A7 minimal renderer/file seam and A1 current-schema restore are concrete open local comparison items where stated—not owner gates. Keep them finite and independently disposition material findings.

For each final decision, reuse—not flatten—the nine dossier fields: **(1)** exact pinned code/tests; **(2)** actual I/O/process/storage/credential boundaries; **(3)** selected license/notice scope; **(4)** actual CR file/input-output mapping; **(5)** commands/fixtures/results/failures/cleanup and E-level by scenario; **(6)** adaptation/dependency/removal/migration/rollback cost; **(7)** measured resources or explicit unknown; **(8)** maintenance/update/platform constraints; **(9)** strongest alternatives, rubric/confidence and independent disposition. Cross-reference detailed receipts rather than presenting this reconciliation as a replacement dossier. Unknown resources/maintenance cannot become zero scores or a root-license clearance. No universal final winner is claimed here.

Completion of these local decisions yields build-ready substantial batches, not a finished live Control Room. Keep separate implementation and E4 checklists so neither all-night research nor live permissions silently replace the user's requested usable product.
