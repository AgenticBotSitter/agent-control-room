# Root review: actual runtime text collection

2026-09-08; baseline4d204e6. Source/receipt review of runtime collector and explicit
exception staging scripts, default and corrected exception outputs, plus independently
reviewed root exception revalidation. No second install or collector rerun by root.

Accept the bounded result: the actual public CycloneDX gatherer processes all39
recorded prepared-pnpm runtime identities at current manifest/lock hashes, finds36
root license attachments across35 packages, then preserves four explicitly staged
retained texts byte-for-byte. The combined identity coverage is39 with40 attachments.
The default four missing cases remain visible rather than being rewritten as native
discovery success. One exception receipt-envelope assumption failed before staging;
the focused correction and successful result are both retained.

The collector maps recorded package-manager paths and asserts installed names/versions;
it does not rebuild the graph. The actual dependency code is invoked through its public
export after the selected implementation hash check. Decoded output bytes are compared
to current source files or exact retained texts, not accepted merely because an SPDX
field is present. Explicit source-location metadata survives exception staging even
though the staging filename is LICENSE. Current packages are never modified.

The filesystem port rejects observed file symlinks and oversized selected reads, but
the lstat/read sequence is not atomic and must not be claimed secure against concurrent
file replacement. This trusted, read-only research checkout is not an untrusted-input
packaging service. A final release adapter must use reviewed bounded/contained reads
and bind the exact inventory, build and file hashes. Those remain implementation work.

This establishes conventional runtime root-text and named-exception collection only.
It does not prove all nested/vendor/README obligations, generated assets, platform
optional variants, development packages contributing bundle code, copied code or
complete redistribution compliance. Postgres source correspondence and the entities
manifest discrepancy retain their earlier narrower provenance statements.

No blocker to using this evidence in the implementation plan. The practical combination
is native pnpm identities plus a maintained attachment collector and existing retained
provenance, with the separately tested bundle plugin for emitted module scope. Final
tool-selection comparison and full-distribution acceptance remain distinct from this
accepted runtime-interface result.
