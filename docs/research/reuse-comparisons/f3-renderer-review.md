# Independent actual renderer source and receipt review

2026-09-08. Reviewed the20-check evidence, actual research harness, acquisition
ledger and retained pinned AgentMarkdown/MediaImage/mediaUtils/lightbox source.
All nine retained source-file hashes and the selected lockfile hash independently
matched the acquisition receipt using read-only filesystem hashing. No candidate
code, test, install, download or browser was run during this review. Root retains
module selection and publication acceptance.

## Narrow disposition

No material blocker to the20 reported DOM observations. The harness executes the
actual selected component bodies, not the former Markdown-span replacement. This
is candidate renderer exercise with synthetic host ports, **not** authenticated CR
integration, native browser/mobile accessibility, real media retrieval or complete
shipping clearance. At initial review the fit narrative still described preflight
only; the author's final narrative disposition is recorded below when available.

## Fidelity and negative evidence

- `react-markdown`, GFM and actual lazy Prism are resolved from the isolated package
  closure. The harness waits for a real `.token` element before testing box-diagram
  fallback; it cannot accidentally call an unloaded highlighter a successful plain
  rendering test. Diff classes and exact copied code are separately asserted.
- Translation and decorative icons are substituted; media, clipboard, external
  navigation and menus use explicit in-memory IPC-shaped ports. `useLightboxClose`
  and portal behavior are actual upstream code. No Electron IPC or actual browser
  clipboard/save permission was tested.
- Expansion bleed is reproduced across two different mounted messages using the
  same source-position block identifier, after a positive expand/streaming case.
  Actual module-global `expandedCodeBlocks` explains it. This is presentation-state
  leakage, not demonstrated disclosure of another message's text or execution.
- Query-suffixed `.png?v=1` becomes the actual DownloadChip, consistent with extension
  matching over the complete source string. This is a real interoperability mismatch,
  not evidence that all HTTP image URLs fail.
- Late old local-media completion is suppressed in the Markdown remount path and
  the current completion is positively acknowledged. Separately, updating the same
  standalone MediaImage instance from one direct URL to another retains the initial
  resolved state. These are distinct cases; neither cancels the other's evidence.
  The fixture does not establish every reused-instance local/remote/error transition.
- One script-shaped HTML fixture produces no script node, and one JavaScript link
  causes no runtime-port call or preview event. HTTPS/relative/mailto positive
  callbacks follow. This supports those exact rendering/dispatch observations, not
  general sanitization, arbitrary URL safety, network isolation or browser XSS proof.
  Direct HTTP image src is present, but JSDOM is not fetching/decoding those images.
- Escape is a synthetic DOM event that closes the actual portal. Focus containment,
  native keyboard behavior, layout/CSS and mobile usability remain untested.

The harness contains no MessageRow approval buttons or CR approval machinery;
assistant prose cannot become a completed task simply because these renderers ran.
No existing result digest/read/review code is removed or replaced by this experiment.

## Resource/provenance limits

The ledger preserves the initial50MiB allocation-monitor stop (npm exit1/SIGTERM)
and explicit root-approved200MiB resume. The resumed install reports153 packages,
exit0 and63,696KiB final/sampled allocation: approximately62.2MiB disk, not process
RSS or a sustained renderer memory benchmark. The initial crossing peak was not
recorded, so50MiB is a threshold rather than a measured exact maximum.

The harness validates source and lockfile hashes before importing candidate code.
That is not a fresh hash verification of every installed transitive package byte.
React19.2.4 is the selected candidate version, not current CR19.2.6 parity. Root MIT
and lock metadata do not establish every transitive original notice or asset right;
the missing `format` license field remains a distribution follow-up, not a runtime
failure. The VM is correctly described as ordinary execution, not a security sandbox.

Both mounted roots are unmounted, tracked copy timers cleared and DOM closed in
finally. The acquisition root remains available for root disposition; this review
neither removes it nor claims its absence. No fixture listener or provider exists.

## Selection implication

Actual Markdown/GFM/highlighting reuse remains credible. Adaptation must address
message-scoped expansion identity, host navigation/media ports and reused-media
state before importing those convenience pieces. The strongest smaller alternative
is maintained react-markdown/GFM directly inside current protected results with
selected code/diff presentation; that alternative is not exercised here. Root can
choose a narrow donor boundary without requiring a new full conversation dashboard.

## Final narrative recheck

The final fit report now records the actual20-check run and approved preparation
resume. Its scope agrees with the source/receipt: real highlighter, three retained
negative observations, synthetic ports, candidate React version, no protected HTTP
or mobile/browser qualification, retained acquisition root. The earlier preflight-only
wording is superseded. No new material finding from this source-only recheck; no
additional runtime execution is requested. Cleanup remains the owning agent's task.
