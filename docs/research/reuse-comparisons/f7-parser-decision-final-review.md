# RC7 focused final source/receipt review

2026-09-08. Read-only independent review of prior findings, corrected fit harness, pin verifier/inventory, recheck receipt and corrected report. No rerun, dependency execution, service, download or product edit.

Both original P2 research findings are addressed within the reported scope:

1. `verify()` executes before dynamic imports of selected parser/decoder/borrowed modules. It reads a fixed expected inventory, recursively inventories declared dependency packages, compares all serialized identities exactly, checks selected versions and refuses package-internal symlinks. The retained inventory and receipt agree on13packages/212files plus seven source/lock hashes. This is an actual pre-import refusal guard, not merely a post-run hash label. Static TypeScript/research-loader dependencies and other application helpers are not thereby claimed as a fully pinned whole execution environment; the report appropriately specifies its selected scope.
2. Candidate entity decoding uses an explicit installed entities2.2.0 root, not `createRequire(resolve('rss-parser'))`. The baseline still imports rss-parser legitimately for comparison. Explicit incumbent-independent resolution fixes the prior research seam; it is not an uninstall/installability proof. Report expressly requires promotion to a supported direct entities dependency/import and retention of copied strip-algorithm attribution before shipping.

Actual retained recheck output records exit0,14/18 minimal parity and18/18 adapted parity, same four material mismatch categories, and borrowed250cap/undated baseline replay checks. Counts match source assertions and inventory metadata. No review runtime was performed, so these observations remain author-executed/direct-receipt evidence rather than independent rerun.

Removal conclusion remains conditional: no product code/dependency removed; potential rss-parser/XML-transitive removal awaits consumer/lock closure and wider maintenance/corpus comparison. Actual decoder provenance/curation and collector remain, and adapted fast-XML candidate is not presented as unchanged borrowed parser or a general rss-parser clone. No remaining blocking finding for the two focused corrections; no whole RC7/F7 completion, full corpus parity or release clearance.
