# F4 acquisition ledger

2026-09-08. Owned root `/private/tmp/control-room-f4.zV8t3e`. Initial disk140GiB free;
task200MiB download scope. No dependency installation or service execution.

Public HEAD discovery via `git ls-remote` pinned Maestro
`2c3baa70ab8f8b2b97c943e4f97c20c5c3e5b81c`, AO
`24e101914976a14145d7302f49626db3541ef8b5`. Initial sandbox DNS failed; scoped public
network escalation succeeded. Both codeload archives exceeded60,000,000-byte curl
caps. The mistakenly attempted partial extraction was rejected by tar and briefly
reached265MiB, exceeding this subtask200MiB target (not global4GiB). This is retained
negative process evidence. Exact owned partial archives/extractions were immediately
removed; root verified0B. No further archive attempts.

Switched to tree indexes (both `truncated:false`) and individually pinned raw source.
First API URL command failed shell glob expansion; quoted retry succeeded. Selected
source receipt JSON records every retained file size and SHA256, including tree
indexes. `f4-fetch.mjs` reconstructs selected implementation/test/license paths via
raw.githubusercontent.com at immutable revisions with15s request/1MB file limits;
initial selected set21 files249,868bytes, then five narrow meeting files. No assets,
executables or dependency scripts. Public web tool also read two repository pages.

Prototype executes only pure TS modules through existing TypeScript compiler and the
existing Control Room synthetic coordinator tests. No long-lived processes created.
Final selected-source root measured1.9MiB; receipt29 files1,923,259bytes (including
metadata). All executions exited; no services were created. Exact owned root was
removed and `test ! -e /private/tmp/control-room-f4.zV8t3e` passed. Sanitized dossier,
hash receipt and research-only reproduction scripts remain. Sources are recoverable
from their pinned public URLs; unsuccessful archive fragments are not retained.
