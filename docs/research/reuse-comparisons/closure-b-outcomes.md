# Independent closure reconciliation: B1–B9

2026-09-08. Requested baseline `bc0a61a` plus local research documents; source/evidence review only. No execution, network, downloads, services, credentials, application changes or new protocol design. This is a finite reconciliation, not final acceptance of the comparison goal.

## Main result

The remaining fleet comparison is concentrated in **work-engine integration/recovery, complete native observation/recovery, and project-bound session observation**. Do not reopen the selected database driver, calendar primitive, fixed-panel loop, task-message authority or detached-checkout primitive just because their implementation is unfinished.

Two closure-table statements are stale when read without the later decisions: RC4's AO-to-CR checkout selection is now DR-08, and RC1's calendar matching is now DR-09. The remaining actual-port lifecycle/occurrence tests mostly belong in implementation. Likewise RC6 does not need an invented production updater during research to prove its existing journal primitive. E3 means a real candidate crossing a representative existing interface, not completing the entire fleet inside a research fixture.

## Per-outcome reconciliation

### B1 — Continuous work, concurrency and batch review

**Settled:** retain canonical claims/attempts/approvals and PostgreSQL transaction authority; DR-10 conditionally selects node-postgres8.23.0 at the bounded database port. Actual pg-boss worker and CR adapter results, DBOS4.27.6 same-session and worker results, and maintained Hatchet source are substantive comparisons—not README impressions. See `f1-pgboss-worker-fit.md`, `f1-pgboss-cr-fit.md`/review, `f1-postgres-transaction-fit.md`, `f1-dbos-worker-fit.md`/review, `f1-hatchet-seam.md`/review, DR-10.

**Genuinely open local discriminator:** actual engine fit for canonical review phases and recovery after uncertain delivery/process restart. Current `task-quality-coordinator.ts:166–176` already releases native capacity before returning waiting_review; compare that real service rather than another synthetic approval table. DBOS durable recv occupies its concurrency slot; pg-boss explicit review phase can free one. Neither observation alone settles full integration cost. Hatchet's post-commit outbox route remains viable: evaluate actual supported acknowledgement/recovery across restart/dedup expiry or establish a precise unsupported seam. Its missing caller-SQL transaction is not a blanket exclusion.

**Implementation acceptance:** continuous eligible pickup, unrelated progress during review, bounded admission, drain, no resubmission of uncertain work, durable review continuation, driver lifecycle races and migration/cutover. **External gate:** actual PG17 roles and continuously operating qualified hosts. Missing local contender evidence is not owner-blocked.

**Batch:** Productive fleet; minimum single-task queue/driver integration belongs in First usable task. One selected engine, not simultaneous adoption of three.

### B2 — Recurrence and unattended bounded policy

**Settled:** DR-09 selects cron-parser5.10 public expansion/includesDate, retaining current grammar, bounded scan, DST/local occurrence keys and policy authority. The42-case corrected comparison and independent review close the matching primitive. Existing occurrence store/outbox is real code, not a timer diagram. Do not repeat its parser contest.

**Open local discriminator:** only the *engine-specific* durable scheduling/restart/missed-run delivery behavior that could change B1's engine decision. Couple it to B1's representative occurrence, rather than requiring every scheduler to recreate the whole policy system. Calculator parity is not occurrence acceptance, but missing occurrence implementation is not a reason to reopen cron-parser.

**Implementation acceptance:** adopt the selected calculator, existing occurrence identities/duplicates and outbox delivery, pre-claim OS/skill/profile/tool eligibility, project budget/concurrency and bounded unattended permissions. Review must not be needed for every low-risk message; consequential effects retain separate approval. **External gate:** unattended host/clock/downtime soak on approved policies.

**Batch:** Productive fleet, dependency on B1 engine and B9 eligibility; no independent second scheduling authority.

### B3 — Codex execution on supported hosts

**Settled constraints/evidence:** actual TS SDK0.153.4 decoder/result fit lacks the native turn ID required by the current App Server observer. Pinned Python SDK `553df1c…` full client/generated models have actual synthetic-peer/observer evidence, not just AST methods. The current direct transport's same-turn early-event refusal is actual negative evidence, not successful recovery. Reviews distinguish offline observer replay/fake settlement from durable online operation (`f2-observer-*`, `f2-python-observer-*`, `f2-current-ordering-*`).

**Open local discriminator:** supported exact-ID state/result/usage recovery and lifecycle ownership at the representative current observer/settlement seam. `f2-async-recovery-source.md` closes one false branch: async wraps the same sync router and is not a separate solution. thread/read can recover stored items/status but does not supply turn usage. Known-ID pre-registration plus separately authorized resume/attributed usage is a concrete candidate, not proven recovery; absent/mismatched usage must remain uncertain. Existing direct transport remains the strongest incumbent, not automatically selected because it refuses safely.

**Implementation acceptance:** implement selected maintained client/transport bridge with bounded environment, deadlines, exact identities, errors/usage, retained result, cancel and no-new-run-on-recovery semantics. **External gate:** fresh exact-version Codex native qualification on each claimed supported OS with its approved authentication and owned process tree. Historical Mac qualification cannot qualify Windows/Linux or another revision.

**Batch:** First usable task (one host) then Productive fleet (additional hosts). Do not make optional harnesses prerequisites.

### B4 — Install/enroll Marvin, Ziggy and Johnny5

**Settled direction:** use native supervisors, distinguished by identity mode: macOS personal LaunchAgent/Aqua; systemd only where available in the actual Linux namespace; Task Scheduler InteractiveToken for logged-in Windows owner context, WinSW for a separately configured dedicated service account. `f6-supervisor-fit.md` reads pinned WinSW2.12.0 implementation/tests and Microsoft APIs, not equivalent runtime evidence. `f6-update-fit.md` executes actual journal/conformance/one-shot cleanup primitives.

**Local comparison remaining:** no need to build an updater to complete a supervisor-source decision. Finalize the stated account-mode conditional dispositions and packaging cost/license checklist; do not pretend a personal profile is a dedicated account. If automatic preservation/update machinery exceeds the bounded native-supervisor composition, identify that *specific* competing reusable function before custom implementation. No generic process-manager shopping.

**Implementation acceptance:** real continuous entrypoint, immutable package/config/state separation, enrollment, upgrade/uninstall and rollback. Source still shows one-shot `run-private-node.mjs`300second lifetime and connector `attempted` guard; historical templates remain deliberately non-installable. **External gate:** actual Windows/Linux/Mac service registration, ACL/profile/account validation, reboot/logout/sleep, process containment and target namespace. These genuine host gates do not excuse missing local packaging code.

**Batch:** First usable task minimum installed manual/one-task path; Productive fleet complete packages; Daily-use release staged updates.

### B5 — Agent/session monitoring and optional terminals

**Settled evidence:** Herdr v0.9.0 actual disposable cat-pane/server reconnect/restart/duplicates and read-only research adapter; Desktop/WebUI actual small components mounted against synthetic protected project reads; renderer comparisons now execute actual Markdown/media implementations. `HERDR_ADAPTER_EVALUATION.md`, `f3-mounted-*` and corrected race review, `f3-journey-*`, `f3-renderer-*`, `f3-webui-renderer-*` distinguish these seams. Current verified-result and review controls remain; MessageRow prose-derived approval buttons are not an acceptable replacement.

**Open local discriminator:** actual Herdr observation into the existing authenticated project/session identity/read surface; no server read-only role is implied by owner-only socket permissions. Complete the *representative* result/transcript/Needs-Me navigation comparison using the actual selected renderer/current protected data. Decide small modules/adaptation cost, not whole dashboards. Existing corrected17-check project race and20-check renderer receipts should not be repeated unchanged. Full WebUI settled/streaming pair has retained negative evidence; strongest smaller option is maintained Markdown/GFM in current verified-result UI with selective presentation reuse.

**Implementation acceptance:** project pages/tabs, open/close finished views without stopping jobs, stale/offline identity, duplicate session handling, keyboard/mobile layout, reload and revoked-identity isolation. **External gate:** real session registration, actual IdP/browser and optional terminal input authorization. Optional interactive terminals must not delay basic task/result readiness, but remain explicitly planned rather than silently removed.

**Batch:** Productive fleet parallel read-only observation; shared First usable task result/attention UI; Daily-use release browser/host acceptance.

### B6 — Reconnect, cancellation and cleanup

**Settled:** reuse native lifecycle/process ownership and current journals/uncertain settlement; no supervisor retry as task recovery. `f6-update-fit.md` proves same journal survives reopen from different working directories, future schema rejection, and actual one-shot close/recover behavior with substituted native ports. It does not prove a two-version updater. Herdr cat cleanup is not native Codex/Hermes containment.

**Open local discriminator:** the shared B3 supported lifecycle/recovery question, including Hermes's actual selected lifecycle where not yet exercised. Count it once, not a second independent RC6 full-client contest. No evidence justifies replacing journals or writing another updater framework merely because the continuous runtime is absent.

**Implementation acceptance:** owned descendant termination or explicit unknown status, late result retention, no repeat after uncertain start, drain/version switch with compatible persistent journals and durable ownership. **External gate:** real OS process trees, forced stop, host disconnect/reboot and credentials/profile behavior.

**Batch:** First usable task minimum cleanup/recovery; Productive fleet continuous drain; Daily-use release version/soak/rollback.

### B7 — Agent coordination and coding workspaces

**Settled:** DR-06 retains canonical immutable task/attempt/receipt authority after actual Maestro changed-payload/duplicate/wake evidence; conditional notification routing is not another task queue. DR-08 selects native Git detached-worktree behind current workspace identity/lease port after actual AO conflict/stale/branch comparisons and independent review. DR-01 retains only the fixed-panel selection loop after unchanged/adapted actual Hermes planner crossed current contribution/receipt interfaces. These strong alternatives were exercised; no README-only rejection of Maestro/AO/Hermes is justified or needed.

**Local comparison remaining:** no new primitive contest is needed absent a named uncovered channel or preservation operation. Reconcile existing saved-discussion/project/task promotion and reviewed-child lineage receipts with the selected boundaries; this is evidence mapping, not a requirement to construct a second protocol. True restart/ownership acceptance of the not-yet-concrete native workspace port belongs to implementation. If that reveals need for substantial automatic stash/merge machinery, reopen AO's already identified selective helpers; do not quietly implement a competing framework.

**Implementation acceptance:** actual detached exact HEAD and path/physical readback, durable exclusive lease, duplicate/conflicting/dirty/stale/failed-create/restart handling without data loss; reviewed contribution/merge process and immutable messages. Current workspace manager's in-memory lease map is not durable ownership. **External gate:** real cross-machine coding workers and explicitly authorized merges/native discussions.

**Batch:** Productive fleet coding/workspace handoff, not deferred indefinitely; shared Idea Lab promotion in Idea Lab and ABS. First non-coding task need not await all coding preservation automation.

### B8 — Cross-machine files and richer artifacts

**Settled evidence:** Hermes remote-retrieval helpers and session download handler are useful distinct transports. `f2-hermes-session-fit.md`/review executes actual handler/FileResponse with synthetic profile/session resolver and real bytes, including mutable file/parent-path negatives. `f2-hermes-intake-disposition.md` confirms current canonical producer/run association already exists; the missing part is a declared export/attachment claim, not all identity. Current final-text receipt cannot truthfully accept arbitrary downloaded binary files.

**Local comparison remaining:** consolidate transport disposition and minimum attachment mapping/cost against the already inspected receipt-store and remote-fetch alternatives. Actual handler selection/path/mutation evidence requested in the intake disposition is now present; do not require building a whole attachment subsystem in research to label that E2 evidence useful. An E3 claim must wait for a legitimate representative intake interface; if absent, identify that implementation requirement rather than fabricate a completed native snapshot. Codex's distinct producer/file route still needs concrete source/interface disposition under B3, not assumed inherited from Hermes.

**Implementation acceptance:** admitted project/job/attempt/run/export association, bounded size/hash/type/path and changed-byte handling, retained bytes/metadata, protected download and review lineage; multiple/binary outputs remain in scope. **External gate:** authenticated actual remote transfer/backend and storage/backup validation. No broad filesystem browser or inferred bot-to-bot sharing grant.

**Batch:** First usable task final-text retention; Idea Lab and ABS richer attachments; Productive fleet producer coverage; Daily-use release backup coupling.

### B9 — Skills, models, capabilities and usage visibility

**Settled direction:** preserve native skill execution and current manifest/profile/eligibility contracts. No competing skill runtime. Hermes and Codex remain required; optional harnesses are separately dispositioned, not claimed supported. Actual usage mapping cases distinguish reported/measured counters, unavailable fields and fake settlement.

**Local evidence gap:** the dossier set reviewed here does not yet constitute a single exact pinned capability/skill/model normalization decision for both required harnesses. Consolidate actual supported metadata/source fields into the existing eligibility/display interface, retaining unavailable/unsupported and estimates rather than asserting every field is measurable. Exercise a representative actual parser/model crossing only where it could change the bridge choice. Do not multiply this into another full runtime qualification or reread every skill repository. Native usage recovery remains the B3 discriminator, counted once.

**Implementation acceptance:** pre-claim OS/tools/skills/profile mismatch refusal; honest supported model/effort choices; visible actual vs estimated usage; observable limit enforcement only; versioned capability adapters and enrollment freshness. **External gate:** actual installed capabilities/authenticated usage on each supported host.

**Batch:** First usable task required profile/eligibility slice, Productive fleet complete inventory/visibility.

## Finite next actions in priority order

1. Finish the B1 engine representative review/restart/uncertainty comparison, including viable Hatchet post-commit semantics. Keep DR-10 and DR-09 settled.
2. Finish B3/B6 supported exact-ID observation/usage/recovery; account explicitly for the separate Codex attachment and B9 capability fields. Async wrapping is already dispositioned, not a new contender.
3. Finish B5 representative authenticated Herdr mapping and selected renderer/navigation decision. Keep current review authority and previously repaired project race evidence.
4. Synthesize B4/B7/B8 conditional choices into sizeable implementation packets, with exact remaining implementation tests and real host gates. Do not reopen these primitives merely to create more research tests.

## Dossier and cost completeness safeguard

This outcome reconciliation references, rather than replaces, the nine-field candidate dossiers. Each final packet must carry: exact pin/files/upstream tests; actual dependency/process/storage/network boundary; exact shipped license/notices; CR input/output mapping; commands/data/failures/real-versus-substituted evidence; adaptation/removal/migration/rollback; measured resource scope; maintenance/update/host limits; and alternatives/confidence. Follow existing reviews and their dispositions, not just report titles.

Remaining unknowns must stay explicit: whole-engine operating/migration cost, production memory and throughput, platform-specific install/process behavior, full selected renderer/media notice closure, additional attachment adapter effort, and complete required-harness capability mapping. Existing single cold measurements and package footprints are not comparable production benchmarks. Do not invent a weighted winner total from missing resource data; reuse responsibility-specific0–5 ratings/ranges and explain unknowns. Production deletion remains zero where a decision only avoids future machinery.

No final license clearance is inferred from a root MIT/Apache label: SDK native/runtime binaries, WinSW dependencies, selected renderer assets and Hermes framework wheel notices remain separate shipped-scope obligations. Conversely, a separate-service candidate must not be costed as if its full source were copied into Control Room. The selected primitives' valid source/test/license evidence is not reopened merely because final release notice assembly is unfinished.

**Bottom line:** B1–B9 are not operationally complete. Their remaining local comparison work is finite and narrower than implementing nine whole subsystems; their implementation/target gates remain substantial and must be visible in the final plan. Root retains final reconciliation and approval.
