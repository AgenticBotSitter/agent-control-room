# Comparison completion audit: next decisions, not another build

2026-09-08. Independent bounded document/source-evidence audit at `2fb66d7`,
including the root's uncommitted closure/implementation/readiness updates. No
tests, downloads, services, application changes or GitHub operations. Root retains
selection and final completion authority. This is not a complete re-review of every
receipt or a claim that the overall goal is complete.

## Main finding

The program defines E3 as an actual candidate crossing a **representative existing
interface**, not a completed replacement subsystem. Several open comparison rows
now mix genuine selection gaps with implementation and target acceptance. Separate
those before commissioning more experiments. Conversely, the unresolved queue and
native lifecycle comparisons cannot be closed merely by writing an implementation
plan: their negative or missing evidence can still change the selected component.

The implementation ledger covers all 26 IDs. Its document-completion checklist still
says five settled decisions although DR-01–07 are present. The machine-readable
index leaves every outcome `comparison_incomplete` with `decision: null`, including
A2 despite DR-05. That avoids false product completion but loses the very distinction
the program requires. Record responsibility-level decisions and remaining local
implementation/E4 gates separately; an outcome can be unbuilt with a settled reuse
choice. Do not change all these statuses to complete wholesale.

## Prioritized decision-changing work

| Priority / responsibility | Smallest useful next action | Stopping boundary |
| --- | --- | --- |
| 1 — RC1 work engine, A5/B1/B2 | Resolve the documented package-local PG setup permission; execute actual DBOS caller-transaction/admission/replay path against existing pg-boss evidence. Finish current Hatchet v1/outbox source seam before deciding whether an actual contender experiment is justified. Use shared canonical identities and named uncertainty/schedule cases. | Select one engine from actual competing behavior and cost. Do not implement a full continuously productive fleet as a prerequisite to this comparison. PGlite migration failures are setup negatives, not DBOS rejection. |
| 2 — RC2 native client, A5/B3/B6/B9 | Compare supported early-event/recovery behavior through the actual observer online, retaining native turn/thread identity and measured usage. Current direct runtime and Python both have ordering negatives; TS exec lacks the required native turn ID. Test the strongest supported reconciliation seam, not another decoder-only happy path. | A defensible client/adapter choice and bounded lifecycle mapping; actual authenticated host runs are E4. Do not invent IDs or silently replace native ambiguity with successful completion. |
| 3 — RC2 artifact intake, B8/C4 | Use the newly mapped actual Hermes session/file route with synthetic profile/session configuration and CR's existing admitted association/intake boundary. Discriminate wrong session/root, changed bytes and producer/run binding. | Decide whether the existing association plus narrow adapter suffices. Do not require a new upstream manifest or a broad filesystem browser by default. Remote transfer remains E4. See `f2-hermes-artifact-fit.md`. |
| 4 — RC5 checkpoint, A4 | Root first resolves the actual object-binding requirement against the inspected OpenBao CAS/metadata interfaces. Then compare the viable adapter's lost-reply/restore/split-commit behavior with existing CR etcd evidence; do not rerun raw CAS/restart checks. | Choose on the real permission/identity tradeoff and recovery cost. Do not construct a fake etcd response from Bao timestamps. Independent production custody is E4, not something two local stores can prove. |
| 5 — RC9 recovery and monitoring, A1/A7/D1/D2 | Complete the bounded Kuma daemon persistence/notification experiment after reviewed offline setup; finish the actual Beszel metrics boundary only for required resource monitoring. Run the outstanding current-schema logical restore/roles/ACL/artifact checks on disposable PG. | Prove the chosen service responsibilities and restoration assumptions. The missing protected app health projection is implementation, not a reason to build a new uptime engine. Do not expand dedicated-DB restoration into shared-cluster pgBackRest deployment. |
| 6 — RC3/RC4 selective presentation/workspaces | For any presentation code actually proposed for adoption, test its still-unexercised real renderer/media closure rather than the substituted Markdown span. For AO, root should disposition attached-branch/path/HEAD-versus-lease mismatches and quantify a thin supported mapping versus retaining current workspace authority. | No whole competing dashboard, general workspace sidecar or complete navigation subsystem is required to close a narrow donor decision. If the donor cannot meet the existing seam without replacing authority, record that precise cost/negative and retain current code with an exit condition. |

These are responsibility-sized packets, not mandatory six new research programs.
Root may close an option from an exact unsupported interface and cost comparison;
the program explicitly permits that, but not a generic “security” rejection.

## Already demonstrated: do not reopen unchanged

- **A2 / RC8:** DR-05 selects `jsonwebtoken@9.0.3` after actual policy, HTTP,
  bootstrap, dispatch, cache and rehearsal comparisons. Typed installation/import,
  regression verification and shipped notices are implementation acceptance; owner
  login/MFA/origin qualification is E4. Neither is another token-library contest or
  a missing login-provider decision.
- **RC10:** DR-03/04 select the prepared pnpm identity graph and CycloneDX public
  text gatherer. The actual client/RSC/SSR plugin build ran and the index records
  independent verification of 113 artifact hashes. The three missing third-party
  texts are already retained. Final aggregation, explicit exceptions, project/copied
  code/assets/external-runtime inputs and negative tests are release integration.
  Do not repeat the miniature plugin demo, reacquire these texts or demand a new
  generic dependency resolver. `f9-build-notice-resolution.md` still says independent
  full-build review pending while the current index reports it verified: reconcile
  the stale sentence against that review rather than schedule another review by habit.
- **RC7:** DR-02 settles retain-both parsing; DR-07 settles Readability/jsdom for
  extraction. Rich reading remains a required C3 deliverable, but building its own
  source-bound detail storage/rendering projection is implementation of the selected
  primitive. It must not become another extractor selection gate unless a named
  failed requirement changes the choice. Miniflux/FreshRSS library interoperability
  and RSSHub's selected publisher route have conditional source dispositions; do not
  invent an existing reader account or a missing publisher to force deployment.
- **RC4:** DR-01 fixed-panel planning and DR-06 task authority versus optional
  notification routing are settled. Maestro disk write/duplicate/ACK distinctions
  and AO real Git conflict/stale/duplicate/advanced-HEAD negatives already exist.
  “Finish actual Maestro delivery beyond writer” in the closure table is stale.
  Those observations do not establish AO-to-CR lease integration, but are not missing
  observations to repeat. The saved source/result/review/revision journey already
  completes the same child with fresh approval and its own result/review; another
  unchanged happy path adds no choice evidence.
- **RC6:** Existing durable journal and close/recover primitives were exercised.
  Native supervisors have explicit personal-session/dedicated-account dispositions.
  Continuous pickup, immutable version switching and fleet drain are honestly absent
  implementation. Requiring a newly built updater before concluding “reuse the OS
  supervisor and existing journal” reverses the approved research/build boundary.
  Actual platform lifecycle/ACL/profile acceptance remains E4.

## C5: accountable, but not a hidden release blocker

The map explicitly makes generic content/media and additional harnesses later work.
Preserve a finite list of already-known candidates (including Claude Code/OpenClaw
and named content/media integrations) with source/interface/license evidence, intended
module boundary, reopen trigger and future qualification. The current generic C5 row
and reader-service dispositions do not by themselves constitute that per-candidate
accounting. Assemble it from the existing dossiers first; identify exact absent
evidence rather than starting a new census. No optional connector needs implementation
or live qualification to finish the first private task or this planning deliverable.
Do not incorrectly apply this deferral to required C3 readable articles.

## Finish the deliverable once the decisive choices are resolved

1. Reconcile closure/index/decision register against latest dossiers. Particularly:
   remove the obsolete RC10 preflight-only next step; link AO-next and current
   reader-service dispositions; attach DR-05 to A2 and DR-07 to C3 at responsibility
   level. Preserve historical failure receipts, not historical “next” wording.
2. Consolidate each selected responsibility's nine mandatory dossier fields by
   reference: exact scope/pins, real versus substituted code, costs/removal/migration,
   bounded resource evidence, maintenance and strongest alternative. Unknown
   sustained memory or effort stays unknown/ranged. No extra benchmark is necessary
   solely to fill a number where the documented uncertainty cannot change selection.
3. Turn the five existing delivery batches into build-ready packets: exact touched
   modules, selected components and alternatives, platform prerequisites, local
   acceptance commands, dependency/review owners, relative effort, rollback and
   update plan. A general “finish RC1–10” first batch is not itself that action plan.
4. Collect E4/owner prerequisites in one list and issue the actual copy-ready
   implementation goal prompt. Missing that prompt and exact batch details are real
   deliverable gaps; they are documentation work, not new product infrastructure.

Root should independently disposition this audit and the remaining decisive local
experiments. Nothing here declares overall comparison, product or deployment done.
