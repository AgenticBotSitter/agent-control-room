# C/D outcome closure reconciliation

2026-09-08. Independent source/document reconciliation at `bc0a61a` plus current
local research, not a runtime rerun, final selection, or product acceptance. Root
retains integration/security authority. This report owns only C1–C5 and D1–D5;
A1–A7 and B1–B9 remain in their companion reconciliations (26 outcomes total).

## Decision boundary

E3 means a real selected component crosses a representative existing interface;
it does not require building its entire replacement product. Reuse existing
receipts, including failures. Do not repeat DR-01 panel planning, DR-02 parser
comparison, DR-03/04 notices, DR-06 Maestro delivery, DR-07 extraction or DR-08
workspace selection solely because implementation remains. Existing report
headings saying those comparisons are still pending are superseded by the decision
register and later reviewed receipts, not evidence of another required experiment.

## Outcome ledger

| Original outcome | Settled responsibility and evidence | Decision-changing local gap (not implementation or an owner gate) | Implementation and external acceptance | Substantial destination |
| --- | --- | --- | --- | --- |
| **C1 Multi-bot Idea Lab conversation** | DR-01 retains the actual fixed-panel loop after mapped Hermes planner comparison; DR-06 retains immutable task/receipt authority rather than Maestro wake acknowledgement. Current roster, turn, budget and recap records stay. F3 actual Desktop renderer and WebUI negative/partial renderer evidence discriminate presentation responsibilities; prose-derived approval controls are excluded. | Shared RC1 durable work/recovery and RC2 native lifecycle choices must support these turns; do not start another planner contest. Root still must record its precise chosen display primitive/modules and adaptations from the completed renderer evidence. No requirement to finish the whole discussion UI to select them. | Wire selected execution/delivery to saved turns, bounded cancellation and synthesis; existing contribution IDs survive restart. Multiple real authenticated participants and actual live cancellation are E4. Display updates must not manufacture execution/review authority. | Idea Lab/ABS batch, dependent on first-task execution; presentation work can proceed earlier. |
| **C2 Idea to active ordinary project** | Retain existing owner decision/promotion, backlinks and project catalog. `tests/idea-lab-owner-decision.test.ts` tests server-derived identity and atomic permit-based promotion; `idea-lab-operator-service.test.ts` creates/runs/synthesizes/promotes with synthetic participants. F3 mounted project/close/revoked-identity cases are reusable. | No distinct donor contest remains for promotion: another dashboard does not replace these domain records. Exact renderer disposition above and shared execution mapping remain; do not label the absent completed first task a missing promotion-library experiment. | Join actual saved decision to its project page, resource eligibility and first ordinary task; preserve IDs/backlinks. Existing promotion does not provision execution authority. Browser/mobile and real completed first task remain application/E4 acceptance. | Idea Lab/ABS batch on first-task infrastructure. |
| **C3 ABS collection and reading** | DR-02 retains attributed Control Center collector/curation and both maintained feed parsers; DR-07 selects Readability0.6.0 plus jsdom26.1.0. Actual corpus/HTML tests, `f7-article-integration-map.md`, reader-service dispositions and selection review distinguish extraction from full reader services. | No fresh parser/extractor/reader-service contest merely because full article detail is absent. Reopen only if a named integration quality/resource constraint fails or an actual missing source needs RSSHub. | Implement the selected source-bound detail/read projection and bounded retrieval, with summary/source fallback and provenance. Existing native result storage must not be populated by inventing completed runs. Current story/source-input alternatives must be reused where appropriate. Plain text avoids claiming HTML sanitization; it does not decide content rights. Approved real feeds/publisher behavior and browser reading are E4. | Selected article packet, then Idea Lab/ABS batch. |
| **C4 Article to research, guide or draft** | Keep collector → proposal → ordinary task → retained result → review → revision services. `cross-journey-child-fit.md` and joined revision evidence exercise same-PGlite lineage, fresh child approval, distinct result, own review and canonical completion/replay. Synthetic execution ports are not native qualification or fleet-wide dedup. | Shared RC2 session/file-to-admitted-result mapping and recovery/uncertainty remain decision-changing where unresolved; RC1 recovery is shared, not another ABS-specific queue. Do not repeat the completed child happy path. | Substitute selected actual execution/intake into the retained lineage; preserve article/project/run/attempt/digest association through interruption. Real sourced output and actual provider/file transfer are E4. Guide/setup/editorial gates remain, with no implicit publish or setup execution. | First-task artifact/revision slice, completed in Idea Lab/ABS batch. |
| **C5 Generic content/media projects and additional harnesses** | `c5-extension-dispositions.md` inventories optional runtime, content/media and peripheral names with explicit future boundaries. `c5-primary-source-screen.md` improves OpenClaw and Claude source/terms evidence. Retain generic project packs and native capability boundaries; do not rewrite private history into public examples. | Finite accounting correction remains: many archived names still lack E1 pins/source/license evidence. Root must classify each as genuinely relevant to a current responsibility (then screen that precise interface) or explicitly outside current selection with a later trigger. A name list alone does not meet the program's E1 rule, but optional connector implementation/E2 is not required now. Do not quietly move B7 alternatives into C5 to avoid evaluating them. | Later opt-in packets require exact runtime/terms, actual interface fit and host qualification before support claims. Claude SDK terms do not license the engine; generic media capability does not select a vendor. No new census or invented binary/provider choice is needed. | Explicit later extension packets; accounting belongs final plan now, implementation does not block first private task. |
| **D1 Backup, restore and retention** | Native PostgreSQL logical dump/restore is selected for dedicated application DB; pgBackRest is complementary cluster/PITR tooling, not a replacement or reason to affect shared applications. Current preflight supplies exact roles/schema/artifact comparison procedure. Root's new PG17 acquisition/signature/mount evidence is setup only. | **Actual current-schema PG17 logical restore is still required local evidence:** source/empty target, ownership/ACLs, restricted-login preflight, representative lineage and real separately restored artifact bytes, with negatives. Toolchain availability does not satisfy it. This is not blocked on production credentials or owner MFA. | Assemble repeatable backup/retention procedure from native tools and existing artifact verification. Production off-host storage, persistent-volume replacement, real keys/custody and target restore are E4. Cluster-wide pgBackRest requires separate scope; no need to deploy it for this logical comparison. | Start in first-task batch; complete recovery/daily-use batch. |
| **D2 Host health, uptime and alerts** | Kuma is the reachability/notification candidate; Beszel is complementary resource telemetry, not task authority. Existing HTTP/classifier observations are narrower than daemon acceptance. `f8-kuma-daemon-preflight.md` identifies exact SQLite/prebuilt, release/dist pairing and persisted checkUpdate=false seams; these are not executed daemon evidence. | **Kuma full disposable daemon/persistence/synthetic notification fit remains**, after explicit native provenance/setup resolution. **Beszel representative metrics/hub/agent fit remains** for required memory trends; source-only Go/API evidence does not qualify it. Do not substitute another eleven helper cases or call unavailable setup a candidate failure. | Add a narrow actual application readiness projection: current private process has no demonstrated JSON `/health` or `/ready` endpoint. Synthetic receiver is explicitly not that endpoint. Protected real probe/alert, actual host trend and deployed persistence are E4. Monitor disable/removal must not alter task completion/lease authority. | Basic fleet operation; finish daily-use release. |
| **D3 Safe updates and rollback** | Reuse OS supervisors, immutable releases and existing durable journals. `f6-supervisor-fit.md` explicitly distinguishes one-task launcher from absent continuous daemon and personal-session from dedicated-account modes; existing journal/recovery evidence remains useful. | No new updater/supervisor contest required merely because continuous service/version switching is unimplemented. Any unresolved native-client lifecycle decision remains shared RC2, not an excuse to rebuild supervision. | Implement continuous pickup and drain/version/rollback composition, preserve journals/results outside releases, and test supported overlap. Never restart the one-task command as automatic uncertain work retry. Actual host service registration/ACL/profile/process ownership and VPS canary are E4. Brief single-VPS outage remains possible. | Productive fleet, then daily-use release. |
| **D4 End-to-end browser, fleet and load acceptance** | Retain existing isolated test runner, memory-safe split tests, actual mounted/browser and joined-service receipts. No new production test framework is selected. | Required local restore/monitoring and shared execution/recovery choices above cannot be hidden in this broad acceptance row. Conversely full fleet/browser/load implementation is not necessary just to choose a primitive with representative E3 evidence. | Build a finite combined acceptance matrix using actual selected interfaces: expired identity, late responses, reconnect, multiple jobs/review waits, restart/restore, mobile/keyboard and sustained bounded memory. Separate synthetic/local evidence from real hosts/providers/browser/account acceptance. No percentage or whole-product pass follows from sum of checks. | Daily-use release; regression cases accompany earlier batches. |
| **D5 Contributor-ready code, licensing and economical releases** | DR-03 native pnpm identity graph, DR-04 public CycloneDX original-text gatherer, actual client/RSC/SSR notice-plugin fit and retained exceptions are selected narrow responsibilities. Full-build review and copied/external scopes supersede older miniature/preflight-only wording. Keep Apache project policy and reviewed substantial jobbers. | No more graph/text/plugin competition needed solely to assemble notices. Missing decisive candidate/license source findings for actual proposed imports remain explicit; research dependency metadata cannot clear shipped bytes. C5 accounting and final build-ready packets/goal prompt are real documentation gaps, not new runtime experiments. | Integrate exact bundled/external/copied/nested-vendor/asset inventory, original notices and negative completeness tests; bind to release hashes. Pin chosen runtime/types/build dependencies and changed-file notices. Preserve generic examples without private data. Batch local checks and selective CI. Actual private/public inventory and publication require separately authorized current remote checks/writes, not assumption. | Cross-cutting every batch; final contributor/release packet. |

## Finite necessary work before claiming comparison closure

1. Reuse the shared RC1/RC2/RC5 reconciliation rather than commission C-specific
   replacements. For C1/C2, root can now disposition the actual renderer evidence;
   failed WebUI streaming/owner checks and its failed acquisition cap stay visible.
   Complete UI implementation/browser acceptance is not a prerequisite to that choice.
2. Finish the three named RC9 local evidence responsibilities: current-schema PG17
   logical restore/artifacts; full Kuma persistence/notification; required Beszel
   metrics fit. Exact source exclusion may eliminate an intended interface, but
   an unexecuted native dependency setup must not be renamed E4 or rejection.
3. Resolve the finite C5 E1/accounting classification above. Preserve every already
   known candidate's disposition, strongest relevant alternatives and missing terms;
   do not expand optional runtime implementation into current MVP scope.
4. Consolidate these results into substantial packets and the promised copy-ready
   implementation goal. Update stale ledger wording/statuses without rewriting
   historical evidence or marking outcomes built because their reuse choice settled.

## Nine-field handoff completeness

Each selected responsibility must reference its existing dossier for (1) exact
pin/files/tests, (2) real path/dependencies/effects, (3) exact shipped license scope,
(4) actual CR seam/mismatches, (5) commands/fixtures/results and failed evidence,
(6) adaptation/deletion/migration/rollback, (7) measured resources with unknowns,
(8) maintenance/update/fork/platform limits and (9) comparative choice/confidence.
The final plan still needs that reference map plus intended-use pass/fail gates and
explained weighted ranges (fit30/effort25/avoided20/maintenance15/resources10).
Do not invent performance numbers to fill blanks: downloaded disk is not RAM,
fixture duration is not fleet latency, estimated glue is not measured engineering
cost. Zero deletion is honest where adoption avoids new infrastructure.

This source reconciliation identifies remaining work, not independent approval of
every underlying receipt, dependency license or final product. No tests, services,
downloads, credentials, application edits or GitHub operations were performed.
