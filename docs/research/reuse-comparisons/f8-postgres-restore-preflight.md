# RC9 PostgreSQL logical restore: exact local preflight

2026-09-08. Source/command inventory only. No downloads, client/server execution, database connection, role/configuration change or service. Read `deploy/BACKUP_RESTORE.md`, deployment instructions, current inventory/check/preflight code, role sources and synthetic recovery/DB tests. This is not restore acceptance.

## Decisive preparation finding

**The existing PG18 disposable server is not sufficient for a current-application restore acceptance run.** The shell resolves none of `pg_dump`, `pg_restore`, `psql`. The known owned embedded package `@embedded-postgres/darwin-arm64@18.4.0-beta.17/native/bin` contains only initdb, pg_ctl and postgres. No broad home/PATH search was performed beyond command resolution and that already-approved root.

Separately, actual `src/web/v1/private-postgres.ts:7–15` requires majorVersion17 and host127.0.0.1 with a nonempty password. Actual `private-database-preflight.ts:223–232` checks server_version_num170000–179999 plus exact session settings and real database CREATE/TEMP privileges. Thus even adding PG18 dump tools would only enable a generic/current-schema-on-PG18 restore experiment; it cannot honestly pass the unchanged current application preflight. Do not fake the server version or metadata to get a green receipt.

## Minimal trustworthy acquisition decision (not performed here)

For the **full current application** target, prefer one isolated, exact PostgreSQL17 macOS-arm64 distribution containing postgres/initdb/pg_ctl **and pg_dump/pg_restore**, plus its required dynamic libraries. psql is useful for the documented migration invocation but is not mandatory if root uses an existing actual PostgreSQL client to execute the same reviewed SQL with stop-on-error. That would still use real pg_dump/pg_restore for archive operations; SQL queries or hand-written JSON are not a substitute for those tools.

Candidate acquisition routes are a signed/pinned self-contained PostgreSQL app/binary distribution or pinned Homebrew PostgreSQL17/libpq bottles with the complete required dylib closure. Neither exact current artifact nor signature/hash/dependency closure has been inspected in this **source-only, no-network** packet, so neither is acquisition-approved or claimed smallest by measured bytes. Root must resolve the actual release minor version, arm64 architecture, public artifact URL, published hash/signature verification, extracted tool versions, dylib lookup requirements and retained size before choosing. Do not install/register an app, package-manager service or global/user binaries merely to get command resolution. Keep extracted executables/cache under an owned root and invoke absolute paths.

If root intentionally chooses a smaller **generic PG18 archive** experiment first, matching PG18 clients (and libraries) suffice with the existing server, but retain the explicit PG17/current-app gate afterward. Avoid an avoidable two-toolchain download if the combined PG17 distribution satisfies both responsibilities. Client/server compatibility is not permission to bypass the app's17-only contract.

## Available current source inputs

- `scripts/private-deployment-inventory.mjs` inventories all lexicographically ordered current migrations with hashes and refuses gaps. Minimal website profile adds `private_web_database.sql`/`private_web_roles.sql`; idea-authoring adds `idea_creation_roles.sql` plus `db/setup/private_idea_adapter.sql`. Inventory is a source check, not a migration runner.
- `db/migrations` contains the application's schema; root should obtain actual fresh inventory/count at dispatch rather than trust the older narrative's64 count. `production_roles.sql` is **not** automatically interchangeable with the narrower private-web roles.
- Current tests/helpers provide synthetic tenant/workspace/owner/project data and Idea adapter setup patterns. The PGlite fixture's database-TEMP metadata override must not be copied to a real PostgreSQL experiment. Existing operations backup/recovery tests exercise contracts/ledgers/synthetic evidence, not pg_dump archives or recovered live objects.
- `scripts/check-private-vps-database.mjs` selects actual compiled website-only, idea-authoring or agent-tasks preflight and reports backupVerified:false/productionReady:false. `private-task-database-check.ts` additionally checks coordinator, result/evidence/session, Idea and optional news roles for its configured mode; a web-only restore receipt cannot stand in for that profile.
- `src/artifacts/v1/native-results.ts` and the artifact storage ports distinguish retained bytes from metadata/results. A database dump alone does not preserve an external file/object store or private integrity configuration.

## Exact finite disposable experiment for root to approve

1. **Target and profile inventory.** Choose one explicit current application profile and immutable source/build inventory. Create a new owned PostgreSQL17 cluster; two distinct explicit database names, source and empty restore; synthetic owner/migrator NOLOGIN role, separate restricted web LOGIN and, if selected, separate authoring/task-role identities. No existing/shared roles or data. A complete agent-tasks claim needs that mode's full actual role inventory, not every role script applied indiscriminately.
2. **Connection seam.** Logical tools may use a private owned socket. To exercise the unchanged production connection constructor, the root runner must separately approve a bounded127.0.0.1 listener on an unused port and synthetic disposable authentication, or label a socket-injected database port as a narrower preflight-only seam. No remote binding, ambient PG configuration, real settings or owner subject is needed for synthetic research. Do not imply a socket fixture exercised the exact TCP connection path.
3. **Current source creation.** Apply each inventoried migration once, under the intended object owner, to source only, stopping on first error. Apply only reviewed database/role/default-privilege procedure for the selected profile. Bootstrap clearly synthetic tenant/workspace/owner identity and a representative project/task/result through actual interfaces or root-reviewed exact fixture seeds. If authoring is selected, include registration and signed records with disposable fixture integrity material; do not replay the fresh registration INSERT over restored data.
4. **Capture consistent expected state.** Stop this fixture's writers, not unrelated services. Record stable row counts plus hashes/IDs needed for schema, tenant/workspace/owner binding, project/task/result and authoring lineage. Record ownership, exact effective grants and database ACL expectations. Store a synthetic artifact's real bytes separately from its DB metadata and retain its digest/size/source-relative locator in the owned fixture manifest. Do not print row contents or reusable secrets.
5. **Native dump/list.** New0600 archive in exclusive directory; explicit host/port/user/source DB; `pg_dump --format=custom --no-password` of the whole dedicated source database. Preserve owner/ACL statements. Record exits, warnings, bytes/hash. `pg_restore --list` is inspection only. No fake SQL exporter, schema-only dump or --no-owner shortcut.
6. **Native restore.** Confirm target empty and distinct. Since cluster roles are outside a database dump, preserve/reuse only the reviewed synthetic roles within this disposable cluster. Run `pg_restore --single-transaction --exit-on-error --no-password` into the explicit restore database, without --create/--clean/--no-owner/--no-acl/parallel jobs. Apply the reviewed database-level ACL procedure separately to restore. Neither a file hash nor archive listing substitutes for this step.
7. **Actual restored checks.** Compare required rows/lineage digests and object ownership/ACLs to the consistent source snapshot. Connect as actual restricted logins, verify current exact application schema/session/grants/preflight and close every connection. Test denied schema CREATE/TEMP, ownership/superuser escalation and mutations outside each role's actual permission set; web roles legitimately write some application records, so a blanket “all writes denied” is wrong. Run both selected authoring/web checks, or all agent-tasks roles if that is the declared profile. Verify source snapshot unchanged.
8. **Artifact and negative checks.** Restore the separately backed-up synthetic artifact into another owned directory; actual storage/read verification must match expected bytes/hash and retained metadata. Missing/tampered bytes must remain failure. Test a corrupted archive only against a separately empty disposable negative target; a failed restore is not authorization to retry a populated target or drop ownership checks. Exact whole-DB/artifact pairing and interrupted transfer/recovery scope must be reported honestly.
9. **Terminal evidence/cleanup.** Retain sanitized source/build/tool/archive hashes, exits, row/ACL/artifact comparisons and each connection/cluster-close result. Remove only the approved exact owned cluster/targets/archive after root review per fixture authority; no shared-role cleanup. Failed cleanup remains uncertainty and retained owned path, not success. No background supervisor, website startup or provider call is needed.

## What remains missing versus already available

Missing: actual native dump/restore toolchain; PostgreSQL17 server for unchanged version gate; root-reviewed runner/profile/role bootstrap and synthetic expected-row/artifact inventory; concrete acquired artifact integrity/dependency receipt; authorized exact transport shape for the application preflight. These are local preparation/design inputs, not a request for production passwords, real owner MFA or existing databases.

Available: migration/role/setup sources, actual application preflight and bounded database code, current artifact interfaces, synthetic fixtures, owned PostgreSQL runner pattern, existing pg client, and logical restore procedure. Keep/reuse them rather than author backup formats or another verification engine.

Success would establish bounded local real logical restore plus the declared current-app/roles/artifact seam. It would **not** prove VPS volume persistence, PG17 deployed topology, encrypted off-host/R2 transfer, actual key recovery, cluster PITR or all other applications' backup safety. pgBackRest remains complementary physical/PITR tooling; it is not a substitute for restoring this dedicated logical database into an empty target. No deployment or final RC9 closure is claimed.
