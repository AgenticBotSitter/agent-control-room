# Joined revision-child independent review

2026-09-08; baseline `1449c64` plus active research-only helper/test additions.
Reviewed stable corrected source after author sign-off, actual native lifecycle,
review submission/service and canonical completion contracts. No test rerun, network,
provider, application change or Git write by this reviewer. Root owns final acceptance.

## Disposition

No blocking source finding for the bounded joined-child experiment. Initial source
omitted explicit positive v2 lineage/canonical-state/completion-replay assertions;
author added these before this disposition. They are substantive: the corrected test
does not rely solely on `complete()` returning without error or a different target ID.

Fresh child authority is not inherited from revision creation. The test first observes
no execution grant, assignment without runs, rejection of the parent approval packet
at the child's actual storage seam, and unchanged parent packet. It prepares the child
binding through the real coordinator and signs a synthetic child job/attempt/operation
approval plus recovery binding. Those packets flow through real admission/handoff code.
This is an authored fixture approval, not an owner-attended or real-host qualification.

The original collector, saved story, research task, retained parent result, changes-
requested review and revision remain in the **same PGlite database**. Child helper
receives that assignment fixture and overrides assignment with the already accepted
child receipt; it does not create another PGlite database or bypass canonical binding
preparation. The local prepared binding must equal the coordinator-prepared digest.

Actual review registration yields `native-review-plan/v2`; deep equality to the saved
revision and explicit from-job/run/target/digest/content/review/feedback assertions
carry parent lineage. Plan prompt assertions preserve exact original instructions and
previous bytes. Real retained-result ingestion, verification, owner review and completion
are invoked with the child target/hash/run. Completion rejects both before verification
and after verification but before the child's own accepted review. Parent review cannot
satisfy that child gate in this path.

Corrected positive assertions read child canonical job/attempt/lease as
`succeeded/succeeded/released`. A second completion replays the exact receipt and leaves
those records unchanged; duplicate artifact ingestion returns the same receipt. Parent
canonical released state, approval and saved child plan remain unchanged. Final effect
counts are one in each local effect store after completion replay.

## Evidence and environment limits

The child uses its own in-memory bridge/run/admission/execution/effect journals and a
distinct fixed synthetic backend run ID. Real signed frames move through arrays and
actual controllers, not sockets. Synthetic transport invokes `wire.authorize()` and
returns authored capabilities/status/output; profile acceptance is stubbed. Result
text deliberately includes source/story/feedback, and structural verification is not
independent research truth checking. Direct services are not an HTTP interaction.

One effect in each **separate** journal is not shared-worker/fleet deduplication,
simultaneous same-node capacity proof or crash recovery. Child native run correctness
is synthetic; no Hermes process, production PostgreSQL, provider or external effect
runs. No source repository or product policy is changed. Helper's explicit initial
clock and distinct synthetic run ID are fixture seams, not policy relaxation.

Cleanup hooks register owned SQLite stores and later handoff/session/bridge closure.
Inspected bridge close only clears connection state and closes the synthetic transport;
handler close is a flag. No concrete closed-store access found merely from hook order.
Successful after-hook execution does not prove every failed-construction point cleans
all allocations; no fault-injected setup cleanup was tested. The same database is closed
by the parent fixture, not duplicated child ownership.

At source-review time the author reported a corrected single-test exit 0 in about
2.31 seconds; finalized direct receipt/report were still being written. This is not
an independently rerun or directly observed terminal claim. Retain exact final evidence
before promoting the research checkpoint; do not infer whole comparison completion.

## Final receipt/report disposition

Reviewed subsequently saved `cross-journey-child-fit.md` and evidence JSON. Strengthened
terminal TAP records 1 pass / 0 fail, total 2309.877292ms (test body 1895.283666ms),
with the child v2/state/replay and separate-effect-count diagnostic. Initial successful
run is separately summarized; the retained initial warning is explicitly sanitized.
This supersedes the pending-receipt status above without claiming reviewer execution.
Final type narrowing/comments/unused imports do not change the inspected runtime path.

Report appropriately distinguishes synthetic owner review, structural verification,
one existing PGlite fixture and separate journals from live/fleet acceptance. Its phrase
“independent-owner review” is an actual owner-role service operation in the synthetic
fixture, not review performed by a real independent person. No new blocking finding.
