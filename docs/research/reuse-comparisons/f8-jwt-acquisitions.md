# F8 JWT comparison acquisitions

2026-09-08. Owner-authorized isolated public evaluation; no app installation.
Storage check: 140 GiB available. Previously retained F1 222 MiB/F2 184 KiB.
This cohort cap: 25 MiB including local npm cache; global cap remains 4 GiB.
Owned root: `/private/tmp/cr-f8-jwt.QxXNpH`.

Initial sandbox metadata fetch failed ENOTFOUND; scoped public-network retry succeeded.
Registry metadata selected exact jose 6.2.12 and jsonwebtoken 9.0.3; package lock
will retain full resolved dependency versions and integrity hashes. npm scripts,
audit and funding calls disabled; user configuration /dev/null and cache owned.
No credentials, personal authentication configuration, native services or app changes.

Install completed exit0,16 packages, scripts disabled. Cohort measured3.6MiB.
Exact resolution/integrities and selected source/license hashes:
`f8-jwt-acquisitions.json`. No package installer scripts ran. npm printed an update
notice; no update was requested or installed. Synthetic comparison initial run and
receipt-capture run both exit0 (69 mapped checks plus10 library-only observations).
Lint initially found an empty catch; a comment-only correction passed lint.
Independent source/receipt review completed. Both test commands exited normally;
the harness starts no services or child processes. Exact owned root was checked
as a real directory with the expected private package manifest, removed recursively,
and absence verified. Final measured cohort3.6MiB; retained cohort now0bytes.
Only sanitized receipts and original research harness remain in the repository.
