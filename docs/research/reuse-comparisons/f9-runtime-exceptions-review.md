# Independent review: four retained runtime notice exceptions

2026-09-08, requested base4d204e6. Read-only source/evidence inspection plus one
authorized invocation of `node research/reuse-comparisons/f9-runtime-exceptions-check.mjs`.
No dependency code imported, downloads, installations, network, credentials,
services, application edits or Git operations by reviewer.

## Disposition

**Accept the narrow four-entry revalidation evidence. No blocking finding.**
Reviewer invocation exited0 with the same four identities, hashes, byte lengths,
eight entities file matches and scope statements as the retained result. This is
not whole-package source equivalence, a generic graph implementation, complete
distribution attribution or legal clearance.

## Evidence distinctions checked

- pg-types2.2.0 and pgpass1.0.5: the script resolves the named entries from the
  retained prepared-pnpm output into this checkout, verifies manifest identity and
  manifest/readme hashes, then checks the full retained MIT-section bytes occur in
  the installed README. Retained sections contain copyright, permission, inclusion
  condition and disclaimer. The1095/1073byte outputs are exact selected text sizes,
  not inferred from an SPDX label or a guessed conventional filename.
- postgres3.4.7: installed name/version and the1212byte retained upstream UNLICENSE
  hash match. The prior artifact explicitly sets packageSourceEquivalenceProvenfalse;
  the new output independently preserves `wholePackageSourceCorrespondence:
  not_proven_by_this_check`. No installed JS files are compared against the upstream
  commit here. The report correctly does not convert version/text matching into
  whole-package correspondence.
- @nodable/entities3.0.0: eight individually named README/code/type files marked
  localMatchtrue in the retained source receipt are hashed against current installed
  files; they match. The1064byte retained MIT text matches the source receipt.
  Entity/package.json is deliberately not one of those eight matching files. Its
  prior source-manifest2.2.0 versus installed/registry3.0.0 discrepancy is disclosed,
  and current source does not claim to reproduce the historical version-only diff.
  That claim remains attributed to prior source research, not newly proven here.
- The installed-path resolver constrains real paths beneath current node_modules
  and verifies each selected manifest identity. It consumes four named exceptions
  from an existing graph artifact; it does not discover arbitrary graph edges or
  replace a maintained license collector. No release file is assembled by this check.

## Nonblocking reproduction improvement

The embedded-text loop restricts every entry to pg-types/pgpass, but does not
explicitly assert the unique pair or final four-entry result set. The checked
current artifact contains exactly that pair and the observed output has all four,
so current evidence is sound. If promoted into a recurring packaging gate, assert
the exact expected unique package/version set to prevent a future missing/duplicated
embedded entry from reducing coverage while the script still exits0. This is not
a reason to rerun the current unchanged research check.

Keep the text sources, original locations, hashes and narrower source-correspondence
limits as explicit packaging inputs. Actual maintained-gatherer integration,
runtime/bundle/copied-code coverage and independent final release review remain
separate acceptance work under root's approval.
