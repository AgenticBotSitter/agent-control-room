# Independent review: public cron-parser calculator adapter

2026-09-08. Source/receipt review only of compare_teamwork's adapter, report and retained first/corrected evidence, current recurrence.ts and pinned public cron-parser implementation/types. No candidate rerun, installation/download, database/service or production edit. Root retains primitive selection, compatibility policy and integration acceptance.

## Disposition

**No blocker found for the bounded42-case parity claim as qualified in the report.** The corrected exit0 and final all-row equality assertion support completion of all42 comparisons; the corrected stdout is truncated and must not be represented as42 independently inspectable raw result rows. The report correctly preserves this limitation and the first failed case. This is not exhaustive grammar equivalence, persistence acceptance or queue selection.

## Exact scope verified

- The first retained output contains42 rows,41 equal, with exactly one mismatch: `0 0 31 2 *` has zero occurrences in both implementations but candidate `safeReason:invalid_schedule` rather than the original empty-success object. This was not an occurrence-count success hidden as parity. The first failure is retained with its assertion exit1.
- The adapted code removes the actual hash-guarded parseField/parseCron span and cronMatches span **in memory**, then changes two verified call-site strings. Both original/adapted sources are transpiled through the same TypeScript tool and invoke exported calculateScheduleOccurrencesV1. The UTC scan, local formatting/key dedup, bounds, once/interval/state behavior remain actual copied source, not a rewritten calculator. No occurrence store/outbox executes.
- Candidate expansion runs real public CronExpressionParser per canonical segment, then unions values and constructs public field classes with original wildcard intent. IncludesDate is actual upstream matching for normal accepted expressions. Legacy singleton-step stripping, Sunday normalization and list union are disclosed compatibility glue, not claimed upstream defaults. No private-method call or locally written numeric range-expansion loop appears.
- The one correction catches only the named exception **at CronFieldCollection construction**, after validated field construction. Actual pinned constructor rejects a sole-month impossible minimum DOM only when DOW is wildcard and no last-day syntax applies. The source pin and numeric-only field gate make the never-match sentinel a coherent compatibility mapping here. The sentinel is explicitly not upstream includesDate support. Other errors remain invalid_schedule rather than silently returning successful empty calendars.
- Restricted DOW with impossible DOM was already a passing OR case, so the mapping does not discard that case. Full-range/list/step wildcard intent, Sunday forms, singleton steps and invalid grammar are represented in the42-case corpus; fractional-minute boundary and range-too-large behavior are retained. Some generated expressions cover only two calendar days, so this is not long-range/calendar-year coverage.
- Equality compares entire serialized result objects, including all occurrence UTC strings/local times/keys and safeReason, not merely counts or output hashes. The first receipt's hashes summarize those actual comparisons. Corrected receipt's aggregate42/42 plus exit0 supports execution, not missing-row independent inspection.

## Cost and limits

The claimed removed source spans total47 lines in this formatting. The first function count was27 compact lines; the corrected function adds the explicit impossible-calendar mapping and is reported35. Those are dense physical-line measurements, not production maintainability/LOC savings. The report correctly retains canonical grammar policy, state/window/identity, and acknowledges public imports/types/tests/notices still needed. Standard expansion/matching is genuinely delegated; custom compatibility responsibilities are not waved away.

Nonblocking follow-ups for implementation:

1. The error-string compatibility mapping is pin-sensitive. Keep the exact impossible-date and rescued-DOW regressions as required upgrade checks; don't broaden catch-to-empty behavior if a future upstream error changes.
2. VM's1000ms timeout covers module evaluation, not the subsequent synchronous calculator calls. This small corpus completed, but it is not a production resource bound or adversarial parser benchmark. Existing window cap and a future typed integration's input/resource policy remain distinct.
3. This keeps the UTC scanner and only replaces its parsing/matching primitives. Iterator-plus-filter remains a potentially broader removal alternative, not proven better; source/delivery work elsewhere must not relabel this as replacing scheduling or canonical durable occurrence semantics.
4. Source hashes guard the reused parser closure/current calculator before candidate use; static TypeScript tooling import is outside that specific guard. No claim of fresh whole-toolchain supply-chain verification is made.

The evidence is sufficient to inform a narrow root decision about this matching adapter. It does not authorize application changes, migrate existing schedule meanings, establish cross-window exact-replay behavior or prove task/approval effects. No additional unchanged run is requested solely to inflate counts or reformat the already-disclosed corrected receipt.
