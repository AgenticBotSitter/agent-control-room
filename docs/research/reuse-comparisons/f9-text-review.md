# Root review: per-package maintained license-text collection

2026-09-08; baseline c437014. Read authored harness, report and receipt. This is
independent source/evidence review, not a second candidate installation or rerun.

Accept the bounded evidence that the actual public CycloneDX library interface
preserves multiple original LICENSE-prefixed files and exact NOTICE attachments,
where the actual plugin hook chooses one license text. This avoids repeating the
failed CLI npm-graph fit: package-path gathering is a different supported interface,
to be paired with the already-selected native pnpm graph, not a custom graph walker.

The authored module list is not an actual Vite build; that separate prior evidence
remains necessary for module discovery. Synthetic strings are not application notice
coverage or legal clearance. The gatherer does not find all possible notice filenames,
deduplicate repeated calls, enforce missing-text failure, or check retained digests.
The checksum assertion is explicitly adapter-owned. These limits are material and
are correctly preserved rather than counted as upstream features.

Actual default symlink following contradicts the optimistic source comment. The
supported lstat filesystem port skips the owned file symlink in this fixture, but
does not by itself guarantee parent-path containment, byte limits or immunity to
file replacement between stat and read. Any final packaging use must apply the
existing bounded/provenance requirements and avoid calling this a sandbox.

No blocking reporting defect found for the narrow comparison. The proposed tool
combination still needs actual39-package original text/retained-exception coverage
and complete build/copied/external attribution. Do not mark RC10 complete, clear
transitive tool licenses, or infer production distribution completeness from this
fixture. Author recorded exact22MiB cleanup and absence; root did not rerun deletion.
