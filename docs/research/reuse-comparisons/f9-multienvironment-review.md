# Independent RC10 multi-environment build review

2026-09-08. Source/receipt and retained-staging read-only review; no install, build rerun, service, application edit or cleanup. Reviewed f9-multienvironment-build.mjs, launch.mjs, staging-prepare.mjs, fit report, terminal/initial failure/partial receipts, artifact inventory and staging acquisition record.

## Disposition

No blocker for the narrow result: the real staged application built using actual Vite/vinext and separate actual license-plugin instances for client, RSC and SSR. **Not full notice completeness, external closure verification or legal clearance.** The report's explicit omissions and corrected staging boundary must remain attached to this result.

## Direct read-only checks

Parsed callback evidence: client records[8]/213 module IDs, RSC[0,20]/660, SSR[0,5]/169. Distinct recorded name@version identities total21. Four identities lack full text exactly as reported: control-room, plugin-rsc, entities and postgres. Build success does not reject null full text because no such policy was enabled. Initial empty server callbacks remain visible and must not become a premature completed notice artifact.

The retained owned root `/private/tmp/cr-f9-staging.ITLsVE` was independently checked against receipts: all113 physical artifact hashes and lengths match, total3110491bytes; all779 recorded staged source hashes match; all1466 dependency symlink realpaths stay inside owned app/node_modules;19800 regular dependency files counted. These checks do not re-audit every dependency's registry integrity or prove a complete operating-system effect sandbox. Root is still present; this reviewer did not remove it.

The terminal receipt has exit0, no timeout/output trigger and2751.62325ms elapsed. Earlier build failure has exit7 and the unresolved styles/control-room.css import; corresponding partial receipt is preserved. These are not overwritten as one success. The corrected preparation list contains that stylesheet plus the four documented component additions. Source dependencies were copied, not silently imported through an original-checkout node_modules link.

## Module discovery versus provenance

The plugin's public callback records package identities discovered through actual build modules. Actual generated chunks include copied Control Center directory modules (client freshness/industry; RSC feed-discovery and others). No separate Control Center package identity appears. Directory location alone does not prove every listed file is upstream-owned: retain the exact existing copied-file/source-modification index rather than treating the whole vendor directory as one license record.

The `externals` field is indeed mislabeled: it filters chunk.imports by string prefix and retains internal output filenames such as `_next/static/...`. It is not a verified external package set. Report correctly says so. Do not feed it into shipping dependency removal or publish it as a complete external inventory. This review does not implement another scanner.

No callback text collision was observed among repeated identities, but there is no deliberate collision negative fixture here. Likewise CSS, favicon, virtual/generated/embedded content and nested license texts are not independently cleared by21 package identities.120 hook output entries versus113 physical files are distinct quantities, not an inconsistency.

## Notices still required

The three missing third-party texts have existing retained source exceptions that root reports independently hash-validating; this reviewer did not reacquire or repeat those validations. Use that retained provenance to assemble coverage, not newly download the same texts. The self record is separate: staging deliberately omitted project/third_party notices. Its null does not mean the original repository lacks Apache text or that third-party exceptions resolve the project's own packaging notice.

The plugin's first-license/full-text behavior established in earlier packets still applies. Successful actual multi-environment execution does not replace the maintained full-text collector, prepared runtime graph, exact exceptions, copied-source notices or asset attribution. Whole release legal clearance remains unestablished.

## Isolation and bounds

Source staging is explicit rather than copying the entire checkout. Preparation excludes dotenv/config/profile paths, uses independent files, relocates verified internal absolute dependency links and verifies their realpaths. This contains the inspected vinext root-relative/dependency-adjacent writes within owned paths; it is not a proof of all possible build-plugin effects.

Build script verifies selected plugin/Vite/vinext hashes before their imports, loads the copied real config through staged TypeScript, and retains its entrypoints/options while adding reporting plugins. Staged cwd/root, configFile:false, envFile:false, owned empty envDir/cache and explicit child environment address the documented env-loading/write concerns. No application output is started.

The launcher uses60seconds,262144 output characters and1024MiB V8 old space. These are not an RSS bound or descendant-process containment. No RSS was measured; disk allocation must not become a RAM claim. The launcher has no dedicated spawn-error handler, so failed startup handling is not generalized by this successful exit. The report correctly avoids broad cleanup guarantees.

Failed absolute-link staging and npm config/DNS attempts are reported with retained acquisition history. First root deletion is author-recorded; current root retention is explicitly honest. No current cleanup pass is claimed by this review.

## Handoff

The retained cohort is **no longer needed for this independent review**. Root may perform its separately authorized exact-root cleanup once any other owner inspection finishes. Do not infer deletion permission for other roots.

Next work can reuse the demonstrated perEnvironmentPlugin API and retained full-text/source records. Artifact-specific completeness, real external closure, collisions, self/copy/asset notices and final distribution validation remain acceptance work—not reasons to invent a general dependency scanner. Root retains final selection/integration/security authority.
