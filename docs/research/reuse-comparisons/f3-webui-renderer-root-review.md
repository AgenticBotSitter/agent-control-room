# Root review of bounded WebUI renderer evidence

2026-09-08 local date. Root read the actual fixture, receipt and final report;
checked the five downloaded source hashes and selected dependency lock hash against
the retained cohort, and inspected actual sanitizer/class behavior at its pin.
No candidate rerun or production edit. Source matching is not a complete installed
dependency or license audit.

Accept the report only as partial/negative evidence. Settled highlighting and
query-string media have positive actual DOM observations. Streaming tokens remain
unmet; the second run's missing src prevents independent-owner assertions from
completing. Source explanation is plausible and matches the selected helpers,
but not proof about every full-page initialization. Do not claim cross-project
leakage, whole-product insecurity, or a passing renderer qualification.

The10MiB acquisition gate failed: logged source plus decoded registry/cache bodies
total17,190,400 bytes. Compressed network bytes are unknown. Root instructed a stop;
all acquisition/runtime ended. No extra allowance retroactively makes it compliant.
Future capped package setup must account for registry metadata as well as archives
before it runs. About38.5MiB retained disk is below the separate150MiB disk allowance;
root measured139GiB free, so this did not threaten available storage.

Root accepts the useful comparison evidence despite the failed gate, not its
acquisition process as compliant. Do not adopt the entire custom settled/streaming
pair unchanged. Prefer the maintained Markdown/GFM presentation candidate at CR's
existing verified-result boundary; separate incremental streaming reuse remains
conditional on a named required behavior and actual message-owned URL adaptation.
No new generic Markdown parser is warranted.

Before cleanup root independently found no open handles with lsof and verified
exact retained root `/private/tmp/cr-f3-webui-renderer.U2Jo3s`. Author reports every
command terminal and DOM/parser cleanup complete. Root authorizes removal of this
disposable acquisition cohort only, preserving authored scripts, source/lock hashes,
download ledger and both failed-run receipts. Removal/absence is recorded separately
after the operation; this paragraph is not itself a cleanup completion claim.

Cleanup subsequently completed: root verified a non-symlink exact directory,
removed only that39,456KiB cohort and confirmed absence (terminal7ca633, exit0).
Authored evidence is retained. Downloaded packages/source/cache can be reacquired
from logged public pins if needed; no user data or application files were removed.
