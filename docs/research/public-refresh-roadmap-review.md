# Public refresh roadmap: independent document review

2026-09-08. Read-only review of README.md, WORK_PACKAGES.md,
COMPONENT_DECISIONS.md and IMPLEMENTATION_PACKAGES.md in the isolated public
candidate at base `cc565d2`, compared with the current private decision register
and the candidate's actual source paths, package scripts and demo entrypoints.
No network, execution, public-clone edits or publication. Root retains security,
license and publishing decisions.

## Result

No publication-blocking disclosure or material licensing/support overclaim found
in these four documents. The new pages explicitly distinguish selected components
from implemented source, synthetic evidence from real runtime qualification, and
unavailable export prerequisites from claimable work. No private host paths,
infrastructure addresses, agent identities, credentials or private research links
were found in the reviewed content. This is not a whole-tree privacy audit.

One concrete instruction clarification is advisable before P1 assignment:

- **P1 production panel versus demo acceptance:** P1 names actual
  `private-app/app/task-results.tsx` and production workspace files, which exist,
  but the public demo enters `contributor-demo/view.tsx` →
  `app/local-preview/workspace.tsx`, not that TaskResultsPanel. Thus the listed
  demo commands alone cannot demonstrate DR-11 production-panel parent/client
  behavior. Add an explicit requirement for an assigned focused production-panel/
  parent harness and command, or deliberately include that panel in a synthetic
  test entry. Do not make a contributor operate a configured private application
  just to test it. Current text already requires actual parent/client cases and
  issue-specific tests, so this is an assignment-readiness clarification, not a
  reason to block publishing a clearly unassigned package.

## Checked scope

- DR-01–11 directions match the private register: retained panel/task authority,
  both news parsers, native graph/text tools, synchronous JWT, selected extraction,
  native detached Git, public cron calculation, conditional pg and minimal
  Markdown/GFM. Database direction remains conditional and queue selection open.
- The documents do not tell contributors to install all proposed dependencies or
  fetch private source. Named P1/P2 starting files exist. Public check:demo/check/
  build:demo/test:demo/test:build:demo scripts exist. P3–P6 explicitly require exact
  available source/fixture assignment before implementation. I did not execute
  those scripts or certify a clean checkout.
- Public README pg-boss wording describes the current integration, while the new
  decision page leaves the eventual work engine open. It does not justify running
  two engines. Native version/platform support remains explicitly unclaimed.
- No authorization is inferred from view closure, prose, quality review, merge,
  demo tests, credentials already present or uncertain outcomes. Work instructions
  preserve leases, dirty work, exact identities, approval and maintained boundaries.
- Apache-2.0 expressly does not relicense third-party code; full actual notices,
  test-only exclusions and unresolved distribution scope remain visible. Component
  versions are proposed targets, not blanket legal/security qualification.
- Economic guidance correctly distinguishes commits/pushes from workflow minutes
  and keeps ordinary local commits permissible. Disabled Actions and issue #1 are
  remote-state statements: this source-only review cannot verify either. Root's
  authorized publication check must confirm current settings/issue assignment;
  no network check was performed here.
- README's historical 30 demo tests/two compiled tests/browser trial are retained
  reported evidence, not independently reproduced by this review. They are
  explicitly synthetic and platform-specific, not a live fleet promise.

No numerical cost/score invention or requirement to repeat settled comparisons
appears. Substantial issues still need exact public base, files, review owner and
acceptance before becoming ready. This review does not approve publication itself.
