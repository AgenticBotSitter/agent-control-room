# Public roadmap refresh receipt

2026-09-08. User explicitly authorized updating the sanitized public application
and component roadmap. This receipt records the completed documentation portion;
the current application source refresh is **not complete**.

- Public repository: AgenticBotSitter/agent-control-room.
- Prior public head: cc565d2acf33883316a5784fd0c8ba56fe2eb389.
- Reviewed PR: https://github.com/AgenticBotSitter/agent-control-room/pull/4.
- Verified merged commit: 42ecea2117852991eacf014b8228551e79a061b7.
- Changed only README.md, ROADMAP.md, WORK_PACKAGES.md,
  COMPONENT_DECISIONS.md and IMPLEMENTATION_PACKAGES.md.
- Eleven narrow decisions, six substantial package outlines, missing source/test
  prerequisites, donation boundaries and remaining open choices are public.
- Independent document review: public-refresh-roadmap-review.md. Incorporated
  its production-panel test-entry clarification before publication.
- git diff --check passed. Documentation-only change; no runtime test claimed.
- Remote Actions permissions verified disabled before and after merge. No
  Actions configuration or deployment changed; no private Git history pushed.

Source refresh still requires the reviewed coherent application/test import
closure, preserved generic public migrations, matching public schema fingerprint,
dependency/notice reconciliation and staged checks. The broad inventory in
public-refresh-source-scope.md is not an export allowlist. Do not replace the
public tree with the private checkout or claim selected dependencies integrated.

Follow-up compiler inventory is saved in public-refresh-closure.json. Preliminary
classification: 534 closure paths, comprising 352 unchanged, 103 additions, 75
updates and four sanitized merges. This remains a planning inventory: five
string-valued Vite entries and runtime assets are outside that automatic closure.
A test-imported operator configuration requires explicit template review; do not
export configuration merely because the compiler can reach it.

Continuation: repaired the existing inventory to parse literal Vite build inputs
without evaluating the configuration. All 15 entry roots are included; the
expanded closure has 546 paths, with only 11 distinct expected generated-output
references unresolved. Two focused parser tests pass. Frozen paths/hashes are in
public-refresh-build-closure.json; older 534-path report remains historical.

Local branch codex/sanitized-source-refresh now contains six fully content-read
Idea UI files. public-refresh-content-review.md records the disclosure review.
An initial copy was rejected for missing per-file disclosure evidence; after full
inspection and a written receipt, the exact reviewed batches were accepted.
Nothing from this source branch has been committed or pushed. It is deliberately
incomplete until its import/schema/dependency closure is prepared and verified.

Next continuation completed the entire 57-file reviewed UI/ABS/vendor cohort;
all candidate file hashes were checked against independent review coverage and
match. Added all six reviewed schema definitions and reconciled the public
preflight to the generated 0064 public digest, retaining the exact catalog
algorithm. Two sequential disposable development checks each passed (64
migrations,145tables,old0058digest reproduced). Source preflight, notices and
remaining source are not yet a buildable release; no new remote push occurred.
Updated donor attribution and retained full licenses. Public candidate whitespace
check passes. Independent web/native full-content reviews remain in progress.

Latest continuation: all66 reviewed web source files prepared and exact hashes
verified. Native/support content review now complete (25+41 files), with unused
Wayfarer CSS held for sanitization. Thirteen additional source/launcher files
prepared before review stopped native-owner-approval-issuer.ts. Root inspected
the entire source and retried once with provenance/no-execution evidence; review
rejected it again for lacking explicit authorization to export the signing helper.
No further retry or workaround. Exact file is now held in the emitter. This is
a publication-preparation authority blocker, not an executed signature or service.
Do not silently omit required source to claim a complete release. Ask the owner
to explicitly authorize exporting that reviewed existing signing-helper source,
without execution, keys, native custody or deployment. Source refresh remains
local, uncommitted and unpublished; the goal is not complete.

Package/lock reconciliation added only fast-xml-parser5.11.0/rss-parser3.13.0 and
their locked transitive records. Offline preparation failed for a missing tarball;
authorized frozen online preparation with scripts disabled completed, reporting
489 reused packages, zero downloads,489 added. Candidate has its own node_modules;
none copied from another checkout. No runtime checks claimed yet.

Unblocked continuation: resolved stylesheet SUPPORT-D1 with installed PostCSS.
Removed133unused consumer-specific selectors, preserved every remaining rule's
declarations/at-rule ancestry, reparsed output, and saved exact evidence. Kept
generic public migration helper/core-schema tests unchanged after exact diff
comparison; updated only generic Idea page/lifecycle/logout assertions in the
compiled handler test. Whitespace check passed. No signing-helper retry, source
publication, real approval or production operation occurred. Owner response to
the explicit signing-helper export question remains absent; do not infer it from
automatic goal continuation.

Further unblocked work: prepared37remaining support-only files (Idea Lab,
persistence, destination/security, fixtures and build config), without retrying
the rejected issuer or continuing the native authority cohort. All40unchanged-
by-sanitization support hashes now match reviewed source; CSS has its separately
verified sanitized digest. First candidate pnpm check exited2 with broad missing
support APIs; after the support pass the same check exits2 with16diagnostics in
managed-native-input/native-approval-packet-store/native-delivery-envelope/
task-assignment-coordinator, including missing native-task-lease-grant and older
NativeEnvelopeChannel signatures. These are incomplete-export findings, not a
passing build or evidence the source implementation is broken. Do not change
types, omit build entries or weaken native approval interfaces to force a pass.
Remaining native source stays unprepared pending the explicit export decision.

Next independent preparation: fully read and included six new generic Idea/news
role/setup SQL templates plus the current private_web_roles.sql definition. No SQL
executed or real roles/grants changed. Updated candidate THIRD_PARTY.md to reflect
the now-included Control Center implementation and parser notices, removing the
old claim that curation source was absent. Whitespace check passes. Signing
helper remains held; no remote source publication or successful build claimed.

Owner explicitly approved exporting. native-owner-approval-issuer.ts and seven
following native source files were accepted into the local candidate. No signing
or execution occurred. Review then rejected server-node-session.ts as runtime
wiring. Root inspected its full existing implementation (privatecommit19e8c12),
documented that only source is copied and ports remain supplied, and retried once.
Review rejected again. File is held; no further retry or bypass. Ask for clarified
approval to export the existing native transport/lease-delivery implementation as
source only, without configuring or running it. Source publication/build remains
incomplete. This is a new exact-file export-review blocker after the issuer was
accepted, not evidence that any live transport was started.

Unblocked follow-up: generic operator-config.mjs source reviewed completely and
included with no actual settings. Public test commands now serialize file workers
and include the three newer compiled tests (multi-project,database-check,node-
launcher); no test was removed. These changes do not run an operator template or
native transport. Remaining server-session export clarification is still pending.

The isolated public clone is retained at
/private/tmp/cr-public-refresh.3pzcHT/repo for this continuation. It contains public
source and the documentation branch, not private history or installed packages.
Retain until the source refresh and remote readback finish; then remove only the
exact owned temporary root after preserving any unpublished work.

## Export approval follow-up

Owner approved exporting again. The exact server-node-session.ts source patch
was submitted to safety review once with export-only scope. Review rejected it
again as signed lease transmission behavior. Restored the helper's exact-file
hold; no alternate copy mechanism, runtime execution, or source push attempted.

Local contributor demo build passed and compiled demo checks passed 2/2.
The remaining demo suite passed 29/30: its native HTTP import inventory still
expects only private-serving.ts but the reviewed Control Center pinned-fetch.ts
also imports the outbound request API. No assertion has been weakened. A precise
outbound-client versus listener inventory review remains required.

Correction to the older retention note above: the retained candidate now has its
own installed dependencies and ignored contributor build products, recorded in
the download log. It still contains no private Git history. Source publication
remains incomplete; only the previously merged documentation update is public.

## Unblocked demo verification follow-up

Reviewed the adopted pinned-fetch implementation: HTTP acquisition uses the named
outbound request API, not a listener factory. Updated the candidate's static HTTP
inventory to retain the exact two source owners and additionally constrain the
news client to request-only named imports, prohibiting default namespace imports
and dynamic HTTP acquisition. No application runtime code changed for this fix.
pnpm test:demo now passes 30/30 (exit 0). Full pnpm check remains exit 2 with 16
diagnostics in native lease/schema interfaces; this is incomplete export evidence,
not accepted application readiness. The rejected server-session remains held.

## Export stopping point

Prepared the five remaining independently reviewed, unblocked node-policy and
node-protocol files through the hash-checked export helper. The helper now reports
zero eligible remaining candidates. No rejected server-session operation was
retried or indirectly performed. Full pnpm check again exits 2, now with 15
diagnostics involving the absent current server-session lease delivery interface.
The contributor demo's 30/30 pass is not full application acceptance.

Source publication is blocked on the repeated safety-review rejection of the
existing server-node-session.ts export, despite owner export approval. Do not
rewrite or stub this functionality to evade the rejection. Preserve the candidate
and resume final source review/build verification only when that external review
condition is resolved. Public documentation remains published; source remains
uncommitted locally. The overall research/publication goal is not complete.

## Explicit copying approval resolved the export hold

Following the owner's explicit approval to copy the identified existing file,
the exact source-only server-node-session.ts patch was accepted by safety review.
No alternate copy mechanism or runtime operation was used. Source and candidate
SHA256 both equal cbb9ae7e28a9b900679ed458870ad589b4e696151d899b0f4f217a22a76fce28.
The previous hold is resolved. Candidate pnpm check now exits 0; pnpm build exits
0 and generates dist-vps without starting the application. Existing upstream
middleware-deprecation and ineffective-dynamic-import warnings remain.
Full compiled-test verification and final publication review remain outstanding;
these successful checks do not establish production readiness or publication.

## Public source pushed — September 8

Completed public pnpm test: 56 passed, 0 failed, exit 0. Final scoped inventory
excluded private research/history/settings, dependencies and generated outputs.
Published 215 changed files in one commit, including all 17 component directions
and updated contributor source-availability notes. Exact reviewed queue runtime
retains one harmless trailing blank line reported by staged diff checking.

Remote branch codex/sanitized-source-refresh verified at
a023659c86b56ff44639c88da86cac63fa03a620.
PR: https://github.com/AgenticBotSitter/agent-control-room/pull/5
PR is open, not merged into main. GitHub Actions enabled=false verified after
push. No private repository push, deployment or live runtime operation occurred.
