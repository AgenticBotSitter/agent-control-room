# CR10A-OPS-010/020/030/040 acceptance

**Verdict:** Accepted locally for the exact value-free, effect-free production reference foundations.
**Date:** 2026-08-29
**Native disposition:** Not attempted and not authorized.

## Accepted deliverables

- OPS-010 deterministic seven-role Compose reference with ten internal exact-peer networks.
- OPS-020 seven hardened Linux systemd unit references with distinct identities and disabled activation.
- OPS-030 owner/node protected-edge routes, value-free provider example, disabled disposition, and fake-only assessment.
- OPS-040 exact resource policy, private factory-created fake adapter, 48-call collector, independent threshold derivation, and read-only projection.
- Exact parsers, canonical digests, secret rejection, accessor/Proxy rejection, and negative-authority fields across every boundary.

## Acceptance invariants

1. Rendered references remain non-deployable and contain no production values.
2. Generated text contains no public port, host namespace, privileged mode, root user, shell, executable secondary command, install section, live provider value, or credential value.
3. PostgreSQL remains the sole writable-volume/path role and migration remains one-shot without restart.
4. Owner and node routes have different authentication and neither exposes a public origin or inbound node listener.
5. Fake provider and health results are evidence candidates only.
6. The health coordinator runs only factory-created in-memory fake adapters and performs no I/O.
7. Resource truth is derived by the coordinator and re-derived by the parser; adapter claims are insufficient.
8. No record grants approval, network, service-control, deployment, or execution authority.

## Verification record

| Check | Result |
|---|---|
| OPS-010/020/030/040 focused hostile suite | 22 pass, 0 fail |
| Combined CR10A focused suite | 47 pass, 0 fail |
| Registered repository pretest | 396 pass, 0 fail |
| Main repository suite | 416 total: 414 pass, 0 fail, 2 intentional platform skips |
| TypeScript and ESLint | Pass |
| Production build and rendered routes | Pass; 2 of 2 rendered-route checks pass |
| Migrations | Pass through migration 0026; 96 PostgreSQL tables verified |
| Diff whitespace | Pass |

Passing these fake/static gates is not evidence that any container, Linux service, provider route, host, or native health check works. The migration verifier used its existing local temporary IPC permission and did not connect to production.

## Remaining owner gates

- Choosing actual host, account, domain, tunnel, service-user, filesystem, image, executable, configuration, and credential references.
- Installing or running Compose, systemd, a protected-edge connector, PostgreSQL, backup tooling, or a health observer.
- Allowing provider, DNS, network, host, credential, database, backup, restore, deployment, or service-control effects.
- Reviewing measured native health, recovery, resource, and monitoring evidence.

## Next safe block

OPS-050/060 may build no-command backup/WAL dry-run planning and a disposable fake point-in-time recovery coordinator. Those implementations must consume OPS-000/040 evidence, preserve claim/marker/receipt and terminal ambiguity, use fake adapters only, and stop before any database, storage, encryption, subprocess, network, credential, or native restore action.
