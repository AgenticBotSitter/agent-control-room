# CR12B-IDEA-100 owner authorization packet — superseded

This packet is historical and cannot be reused. IDEA-105 changed the exact reviewed Hermes revision, compatibility
commit, source-evidence digests, panel packet, and qualification-plan digest. Use
`CR12B_IDEA_105_OWNER_AUTHORIZATION.md` only after its integration and no-effect prerequisites are satisfied.

The repository packet is ready. Nothing native has run yet.

If you want to start only the disposable Hermes 0.21 qualification, authorize exactly this scope:

> I authorize one owner-attended disposable read-only Hermes 0.21 Idea Lab qualification on this Mac using my existing
> Hermes authentication: at most one native attempt and one provider call, at most five minutes, zero tools, MCP servers,
> or plugins, a temporary empty profile and Git workspace, sanitized evidence only, required cleanup, no repository
> writes, no deployment, no live Idea Lab panel, and no retry after uncertainty.

After authorization, Codex will first perform a no-effect preflight and give you the exact attached-Terminal command. You
must run that command yourself and handle any macOS Keychain prompt. Codex cannot type the owner phrase, approve Keychain,
or claim that you are present.

This authorization does not approve receipt acceptance or a live panel. A successful native result is only an
independent-review candidate. The live panel later needs a separately stated, fresh owner window bound to one exact Idea
Lab session, participants, budget, admission, and run.
