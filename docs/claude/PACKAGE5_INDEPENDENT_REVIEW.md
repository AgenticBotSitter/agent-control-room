# Package 5 independent security review

Reviewed Claude's `0de4dfa2`, `4afc91ce`, and `085cef09` against `3898b1f9` on an isolated review branch. This is a review record only; Claude's implementation has not been changed here.

**VERDICT: CHANGES**

1. `db/migrations/0086_mac_local_owner_review_profile.sql:60-64` — The guard describes an owner-authored fixed profile, but it accepts any human identity in the same tenant. A second human identity with no owner grant can be named as `createdBy`. The negative tests cover an agent, an unknown identity, and a different tenant, not this case. Bind the author to the active owner identity/grant or narrow the claimed authority and prove that another human cannot use this insert privilege to gain an owner-only effect.
2. `db/migrations/0087_mac_local_fleet_signals.sql:24-27` with `db/roles/task_coordinator_roles.sql:63-67` — The coordinator grant can insert/update readiness records for any node whose payload claims `macos`, `mac-local/v1`, and `local-only`. The guard does not bind the node to the three provisioned local worker identities, active state, or the installation tenant. A local-looking node outside this installation can receive an arbitrary `verified` signal and future expiry. Constrain the permitted node scope to the first-owner provisioned workers and add negative tests for other local-looking nodes/tenants. State explicitly whether the coordinator login is trusted to report readiness; the SQL guard does not independently prove a signal came from a pinned executable.

Other reviewed areas: the schema fingerprint excludes only the schema-owner-owned migration ledger; the task-host queue option is used by the database check; the denied-write probe requires PostgreSQL SQLSTATE `42501` on a separate connection; and the key-pin read moved to the coordinator login. Source inspection found no separate concrete regression in these areas. The guard findings above prevent approval.

Disposable PostgreSQL 17 rehearsal: `mac:rehearsal up`, `mac:prepare-task-runtime` with the recorded local Hermes choices, and `scripts/mac-local/rehearsal/section13.ts` passed. The section 13 checks and three-worker readiness passed; no task was submitted. The temporary PostgreSQL and local service stopped, both local ports were confirmed closed, and the isolated rehearsal directory was removed. A separate `mac:rehearsal down` reported PostgreSQL already stopped because section 13 had performed its own cleanup. No live database or Tailscale change was made. Passing rehearsal does not waive the two permission-boundary findings above.
