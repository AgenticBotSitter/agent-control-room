# CR9B-WF-080 acceptance

**Disposition:** Accepted as an explicit disabled disposition
**Date:** 2026-08-29
**Measured Unreal benchmark:** Not attempted and not authorized

## Accepted boundary

1. One exact packet binds the Wayfarer pack, model-render stage, private-scene identity policy, 1920 by 1080 by 300-frame workload, one warm-up, three measured runs, median aggregation, and fixed metric/evidence requirements.
2. The packet enforces a 15-minute runtime, 16 GiB memory floor, 64 GiB scratch floor, one attempt, zero provider cost, and forbidden network.
3. The packet stores no path, scene/render bytes, credential, usable locator, tool location, or raw native output and grants no native execution authority.
4. Thirteen ordered readiness gates separate a frozen packet from private scene, tool, executor, node, hardware, GPU, scratch, network, measurement, integrity, cleanup, and owner-window evidence.
5. Complete prerequisites create only a candidate for a fresh owner-attended window. They do not authorize or execute a benchmark.
6. Evidence schemas distinguish measured pass, measured failure, blocked-before-start, and post-start ambiguity. A pass is only an independent-review candidate and cannot activate Unreal or resolve media completion.
7. The authenticated append-only SQLite ledger preserves disabled truth across restart and detects record, metadata, key, and schema tampering.
8. The Wayfarer project workspace now shows one of thirteen gates met, all twelve named blockers, fixed ceilings, and zero attempts with no action control.

## Current truth

Only `exact_benchmark_packet` is met. Every native prerequisite is missing. The accepted disposition is disabled with no attempt, scene read, GPU work, output, network, approval, or external effect. No installation, download, Unreal launch, native process, host inspection, storage access, media access, credential use, or provider call occurred.

## Verification

The combined CR9B gate passes 62/62, including 10 WF-080 contract and durability tests. Repository pretest passes 318/318. The main suite reports 416 total, 414 passed, zero failed, and two intentional platform skips. Type checking, full lint, production build and rendered routes, all 96 PostgreSQL tables through migration 0026, responsive browser verification, and diff validation pass.

## Residual gates

- The ledger needs an independently protected high-water checkpoint before live use.
- A real benchmark requires current private scene/tool/node/environment evidence, a reviewed native runner, node approval attestation, network enforcement, evidence/cleanup qualification, and a new exact owner-attended authorization.
- WF-090/100 may implement only a frozen disabled executor and upload-preparation package against this contract. Missing measured evidence must keep Unreal execution ineligible.
- Upload and publication remain separate destination-idempotent owner-controlled gates.
