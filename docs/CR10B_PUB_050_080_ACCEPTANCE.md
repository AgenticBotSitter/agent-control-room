# CR10B-PUB-050/060/070/080 acceptance

This block is accepted locally only when:

- the fifth workspace candidate is private, fabricated-only, in-memory, network-disabled, and non-authorizing;
- all public guides cover their supported meaning, failure handling, security limits, and postponed integration boundaries;
- every local Markdown link resolves;
- public schemas freeze no-effect constants;
- the isolated public-workspace descriptor enumerates all five local candidates while the repository preparation policy remains unchanged;
- the release plan is exact, digest-bound, deterministic, ordered, and effect-free;
- re-digested identity, order, tree, observation, and cross-plan substitutions fail closed;
- matching independent synthetic observations produce only `synthetic_candidate_only`;
- failure, correlated runners, or output mismatch remains blocked;
- the materializer stops before archive creation and contains no client;
- focused and full repository verification pass.

This acceptance does not authorize an archive, package installation, registry request, key use, signature, provider/native-harness call, upload, repository-visibility change, or publication.

## Local verification recorded

- stage zero: ready, with the original lock digest and exact build policy restored;
- `npm run test:cr10b`: 41 passed, 0 failed;
- `npm test`: pretest passed; main suite 414 passed, 0 failed, 2 intentional platform skips; posttest 14 passed;
- TypeScript check and full lint: passed;
- production build: passed;
- rendered-route checks: passed;
- migrations through 0026: 96 PostgreSQL tables verified;
- diff validation: passed.
