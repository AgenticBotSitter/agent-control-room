# CR12B-IDEA-070 future live-panel owner packet

**Current disposition:** Blocked before native contact. This document is preparation, not a request to enter a code or
approve a Keychain prompt, and not authorization for Codex or any agent to contact Hermes.

## What the future owner window will cover

Only after the default-disabled driver and qualification harness are implemented and reviewed, Control Room may ask for
one new exact owner decision covering one disposable Hermes 0.21 qualification and, if that succeeds, one bounded Idea
Lab panel session. The later request must name the exact commit, tree, installed runtime identity, profile/workspace,
participant count, call/message/cost/time limits, evidence destination, cleanup steps, and maximum elapsed window.

The eventual attempt must remain owner-attended and single-use. It must use Hermes-native protected-value custody;
Control Room may receive only a custody-evidence digest and may never read, copy, log, or persist the underlying value.

## Gates that are still closed

1. PR #180's exact Hermes 0.21 compatibility contract must be integrated without pin drift.
2. A provider driver and native qualification harness must exist and remain absent from default/browser/production
   composition.
3. The exact installed runtime, adapter, method surface, zero-tool/MCP state, and sanitized-output behavior must be
   qualified.
4. A protected-value custody receipt must prove harness-native resolution without exposing material.
5. Every participant must have an exact runtime/profile/conversation binding.
6. The repository must pin the accepted qualification receipt and admission digest.
7. A fresh strong-factor-backed owner effect window must be created for only the exact panel.
8. Cleanup and unknown-outcome reconciliation must be executable before the first call.

Until all eight gates pass, the accepted native-receipt list and accepted-admission list remain empty and provider
contact remains disabled.

## Hard stop conditions

Stop before a call if the version/revision/methods differ, tools or MCP are present, the owner window is absent/expired,
participant binding is incomplete, a limit differs, protected values would cross into Control Room, output cannot be
filtered, or cleanup/reconciliation is unavailable. After a durable call marker, timeout, disconnect, malformed output,
or unknown provider state is terminal ambiguity. It is never retried automatically.

No panel result may create a project. The owner must separately review the synthesis and use the existing project
promotion decision.
