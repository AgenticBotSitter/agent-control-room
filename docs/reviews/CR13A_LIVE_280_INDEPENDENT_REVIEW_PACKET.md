# CR13A-LIVE-280 independent review packet

**Review type:** different independent, report-only, zero-repair
**Required model:** `gpt-5.6-sol`
**Required reasoning effort:** `xhigh`
**Product:** `c1743b7f7b5c8362cec3d33b149e3f51c5e5fda6`
**Product tree:** `2dc81b05566c5c9bb3adc3657f597c2d596d2b14`
**Design parent:** `f23be84ef28d1dd6e24eff42194f6727635f5a53`
**Accepted LIVE-270 product:** `5e5384b1c7b3806a62672018843aa318b0e75728`
**Accepted LIVE-270 review SHA-256:**
`96edcc2c9c65ea38b3da1adec7c092fdf056e544f02856705e93ce0f19d79609`

## Authority and stop boundary

This review is local, repository-only, read-only, and effect-free. It authorizes no install, download, product repair,
repository edit, native issuer import or invocation, shell retrieval, candidate assembly, owner-window creation or
spend, listener, locator, resource, provider, credential, persistence, process launch, network contact, deployment,
DNS, hosting, or production action.

Do not generate a review program. Do not alter the product, packet, dependencies, or test selection. Run every fixed
command exactly once and in order. Any command failure, identity mismatch, unsafe import, hostile execution, unexpected
consumer, nonzero effect, false blocker clearance, or incomplete cleanup rejects the product. Do not retry, repair,
substitute, or broaden the sequence. Return a prose report only; make no edit in the shared repository.

## Required independent inspection

Before the fixed sequence, inspect the architecture, ADR-179, exact product range, new module, safe barrel, package
scripts, and dedicated tests. Verify all of the following:

1. The product has the exact parent and tree above and changes only `package.json`, the safe connection-registry barrel,
   the new candidate-contract module, and its dedicated test.
2. The contract binds the exact accepted LIVE-270 product and review digest.
3. Component, stage, blocker, and ceiling sets have exact membership, order, uniqueness, and frozen identity: 15
   components, 10 stages, 14 blockers, and 21 ceilings.
4. Repository fakes, caller readiness assertions, and public candidate assembly cannot satisfy a real prerequisite.
5. All per-operation ceilings are one; automatic retry, retry after uncertainty, replacement resources, and reopen are
   false.
6. The product records accepted repository source but cannot self-accept, assemble a candidate, create owner authority,
   claim a physical attempt or qualification, wire runtime use, or become activation eligible.
7. All 31 actual totals are exactly zero, all eight authority grants are false, and no external effect is claimed.
8. Contract/status records, arrays, callable exports, errors, and the error prototype are frozen and exact-provenance;
   copies, Symbols, accessors, Proxies, hostile extra arguments, and ambient intrinsic replacement execute zero caller
   behavior.
9. Public evidence and errors contain no address, port, endpoint, host/user/process/path identity, credential, key,
   owner phrase, locator, capability, resource, command, environment/provider content, native diagnostic, or stack.
10. The module imports only established security helpers. It does not import the native issuer/driver, `node:net`, host
    observer, process, filesystem, child process, SSH, credential, database, HTTP, signer, timer, provider, or deployment
    module and performs no dynamic import.
11. The safe barrel is the sole production source consumer. There is no route, API, UI, worker, scheduler, startup,
    Idea Lab, Hermes, native-module, or deployment consumer and no exported assembler, provider, callback, shell,
    factory, capability, locator, resource, or command.
12. Cleanup removes the exact disposable review root and verifies its absence.

## Fixed disposable review protocol

Use a fresh local-only disposable clone detached at the exact product. Copy the already prepared dependency tree from
the source checkout without installation or network access. Record the exact disposable root. Run these commands once,
in order, from the detached clone:

1. `git status --short`
2. `git rev-parse HEAD`
3. `git rev-parse HEAD^{tree}`
4. `git diff --check f23be84ef28d1dd6e24eff42194f6727635f5a53 c1743b7f7b5c8362cec3d33b149e3f51c5e5fda6`
5. `node scripts/qualification/platform-key-store-stage-zero.mjs --platform macos`
6. `npm run check`
7. `npm run lint`
8. `npm run test:cr13a-physical-qualification-candidate-contract`
9. `npm run test:cr13a`
10. `npm run test:build`
11. `npm run db:verify`
12. `git status --short`

After command 12, remove only the recorded disposable root and verify that exact path is absent. Do not retain copied
dependencies, build output, review artifacts, native resources, listeners, sockets, timers, or other disposable state.

## Required disposition

Report High, Medium, and Low findings separately. Acceptance requires exact identity, all twelve commands passing once,
all twelve inspection groups passing, 0 High/Medium/Low, zero hostile behavior, zero external effects, clean initial
and final status, and verified cleanup. Acceptance permits ordinary integration of this inert repository contract only.
It grants no real-provider, candidate, owner-authorization, native, physical-qualification, runtime, deployment,
blocker-clearance, or production authority.
