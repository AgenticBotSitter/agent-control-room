# CR13A-LIVE-420 independent review packet

**Review type:** different independent, report-only, zero-repair
**Required model:** `gpt-5.6-sol`
**Required reasoning effort:** `xhigh`
**Product:** `c1287817079e6951ab5d1fbe24829cccc517687d`
**Product tree:** `8ea4b1350ab619008598332f634c3348815c06f2`
**Design parent:** `2a10eb72faf1abf262c9383442a723638bab7d0c`
**Accepted LIVE-410 product:** `e4d58ff35a44e66454cae8e778b31362902dab6b`
**Accepted LIVE-410 review SHA-256:**
`c3f79f0ad2634a2bcbb0abd39eeb21c1b54154e1389a020b0839343f3ffb0bbf`

## Authority and stop boundary

This review is local, repository-only, report-only, zero-repair, and non-native. It authorizes no install, download,
shared-repository edit, source invocation, descriptor/process/OS/host/path/environment read, raw observation,
attestation, listener, provider, network, production database, deployment, DNS, hosting, or production action. Local
PGlite may exercise synthetic authorization registration, spend, recheck, and the private lookup implementation, but
must never invoke the retrieved source. Run the fixed commands exactly once and in order. Any failure or inability
rejects. Do not retry, repair, substitute, broaden, or write a report file; return prose only.

## Required independent inspection

Inspect the LIVE-420 architecture, ADR-193, exact product range, source-owning module, predecessor tests, new focused
tests, package scripts, and accepted LIVE-410 continuity. Verify:

1. Exact parent/tree/product and exactly five changed paths: `package.json`, the source-owning module, the LIVE-330
   source test, the LIVE-410 contract test, and the new LIVE-420 test. Independently hash the accepted LIVE-410 review.
2. The implementation binds the exact accepted LIVE-410 product and review and reuses the accepted LIVE-330 source
   singleton plus the exact accepted authorization-store spend/recheck primitives. It does not call or parse the
   public LIVE-400 composition result.
3. The same JavaScript module owns source creation, private `WeakMap` storage, the exact map key, final authorization
   spend/recheck, and the only lookup. No source, map, key, getter, callback, continuation, receipt, success capability,
   store instance, replacement binding, or readiness boolean is accepted or exported.
4. LIVE-330 truth is accurately superseded: the source remains private, frozen, stored once, unexported, and
   uninvoked, but is retrievable only after the same flow's exact fresh spend and successful immediate database-time
   recheck. The implementation and its factory remain absent from the connection-registry barrel.
5. A successful synthetic flow constructs the exact store internally, spends once, rechecks once using the same
   sealed authorization and exact lexical receipt, performs one captured `WeakMap.get` using the module-owned key,
   accepts only the exact frozen module-minted source, clears lexical references, and stops before invocation.
6. Pre-spend rejection, commit-return uncertainty, already-spent evidence, recheck failure, source absence, source
   substitution, and every post-spend error are terminal. There is no retry, replacement authorization, refund,
   unconsume, fallback, second lookup, or alternative authority route.
7. Exact replay and concurrent entries produce one durable fresh spend/recheck/lookup total. Expiry, database failure,
   commit uncertainty, and mid-flight caller mutation cannot cross the lookup boundary. Hostile input and dependency
   accessors or Proxies execute no attacker behavior.
8. Implementation/status/result parsers accept only exact frozen records and reject copies, extras, accessors,
   Proxies, Symbols, alternate or inherited state, and ambient intrinsic replacement. Exported records, arrays,
   parsers, errors, factory, runner, and runner method are frozen as applicable.
9. Public results and errors are sanitized and expose no authorization, receipt, database row/time, nonce, source,
   map/key, callable, descriptor, process, path, host, credential, locator, command, raw code, diagnostic, or stack.
   Every result is terminal, non-authorizing, and reports zero invocation, native read, raw observation, and effect.
10. Static source status retains 34 zero actuals and eight false grants; static LIVE-420 status has 22 zero actuals and
    eight false grants. Dynamic success reports one lookup only, while all results report zero source invocations,
    native reads, observations, listeners, network/provider calls, production contacts, and external effects.
11. Exactly one guarded source-map lookup exists. There is no call of the private source, barrel export, application,
    API, worker, scheduler, Idea Lab, Hermes, startup, or production consumer, new migration, native listener, provider,
    network client, real PostgreSQL/VPS use, or deployment path.
12. Cleanup removes the exact disposable review root and verifies its absence with no dependency/build/review residue.

## Fixed 14-command sequence

Use a fresh local-only disposable clone detached at the exact product and copy prepared dependencies without install or
download. Record its exact root. Run once and in order:

1. `git status --short`
2. `git rev-parse HEAD`
3. `git rev-parse HEAD^{tree}`
4. `git diff --check 2a10eb72faf1abf262c9383442a723638bab7d0c c1287817079e6951ab5d1fbe24829cccc517687d`
5. `node scripts/qualification/platform-key-store-stage-zero.mjs --platform macos`
6. `npm run check`
7. `npm run lint`
8. `npm run test:cr13a-atomic-source-lookup-composition`
9. `npm run test:cr13a`
10. `npm run build`
11. `node --test tests/rendered-html.test.mjs`
12. `node --import tsx scripts/verify-migrations.ts`
13. `git status --short`
14. `git diff --check 2a10eb72faf1abf262c9383442a723638bab7d0c c1287817079e6951ab5d1fbe24829cccc517687d`

After command 14, remove only the recorded disposable root and verify exact absence. Command 12 is the listener-free
local PGlite verifier and permits no real PostgreSQL service, network, or production contact.

## Required disposition

Report High, Medium, and Low separately. Acceptance requires all twelve groups and fourteen commands to pass once,
exact identities, exactly five changed paths, 0 High/Medium/Low, 13/13 focused, 439/439 CR13A,
producer-supplied current-turn 769/421/392 full lifecycle, 5/5 build, 4/4 render, 38 migrations/124 PGlite tables,
34 and 22 zero static actuals, all authority grants false, one successful synthetic private lookup, zero source
invocations/native reads/listeners/network/provider/production/external effects, clean status/diffs, and verified
cleanup. Distinguish producer evidence from independently rerun commands.

Acceptance permits ordinary integration of this exact unwired private lookup composition only. It grants no source
invocation, descriptor/process/OS/host/path read, raw observation, attestation, signer, replay checkpoint, candidate,
owner authorization, physical qualification, runtime activation, provider, production database, deployment, DNS,
hosting, blocker clearance beyond private lookup implementation, or production authority.
