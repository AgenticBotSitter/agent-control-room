# CR10C-MECH-010/020/030/040 acceptance

This local block is accepted only when:

- the inspector is restricted to the eight frozen public roots and rejects symlinks and special entries;
- its evidence has no source bodies, private values, release authority, or effect client;
- the complete source inventory is path-sorted, class-bound, digest-bound, and covers every public class;
- the five local candidate manifests and their direct dependency graph are exact;
- each candidate has an exact LICENSE and NOTICE digest, without claiming a legal disposition;
- both public schemas, both fabricated fixtures, and every local Markdown link are normalized and constrained;
- sensitive-value findings are digest-only and force a blocked state;
- re-digested root, dependency, link, record-order, identity, and Proxy substitutions fail closed; and
- focused tests, full type checking, lint, repository tests, build, rendered-route checks, migration verification, and diff checks pass.

This acceptance does not authorize a legal license conclusion, public-tree disposition, archive creation, package installation, registry or provider contact, network access, signing, upload, publication, repository-visibility change, real clean-room installation, or release certification.
