# Same-subject revised result and protected review: acceptance

2026-09-05. Reviewed production `b4a29b7`; final integrated tests `5abdc5b`.
Base PR #333. Contract: `CR14C_REVISED_RESULT_CONTRACT.md`.
Excluded draft PR #329 remains unaccepted and is not in this branch.

## Delivered

The durable follow-up plan now binds a separately approved synthetic child execution to
the original logical review subject. Its new native review plan has a separate v2 HMAC
purpose and retains exact predecessor result/review/finding identities. Registration
checks actual predecessor bytes/evidence before accepting the discovered child run.
Submission retains the actual child job/run/attempt/node/artifact, creates the exact
successor target and revision using the existing Completion Gate, and stages checkpoint
publication. The old target becomes superseded; the new one inherits no review or checks.
Existing v1 plans, tags, targets and receipts are unchanged.

Protected child-job results now show that child's actual file with the shared logical
review history. Owner review and manual verification retain the child producer ID in
routes and receipts while checking the authenticated logical-subject relationship.
Original-job routes retain their own files. Matching files to targets no longer relies
on a coincidental content hash or node identity. Read-only access to native review plans
is added to the private-web role and exact preflight; no execution-plan key/read or
native-plan write permission is added. No production grant was executed.

The trusted execution planner now reads v2 plans and binds them through the distinct
revision registration path. Planning remains non-executing and reports separate
assignment/approval requirements. Fresh quality verification, owner review and canonical
completion compose for the child through the existing optional coordinator and protected
compiled app. Native execution/runtime registration are not mounted by this acceptance.

## Independent review and retained corrections

Root authored the normative contract, production and SQL-role change. Separate agents
authored the synthetic two-job fixture/native regression lane and protected/compiled
regressions. An independent reviewer found a real association bug: an ancestor artifact
could be paired with a later revised target if content cycled A to B to A on the same
node. All native plans now require their exact target, and absent-plan legacy fallback
is limited to initial targets. The result list uses the same predicate. The reviewer
reproduced both corrected refusals and the preserved positive initial paths, reporting
no remaining actionable production findings at `b4a29b7`.

The fixture's first child-assignment run failed before native start because the seed
already had an unrelated active reservation in addition to the original job. Root
authorized capacity three only in the new synthetic child route, with assertions that
both prior reservations remain unchanged. Nothing was expired, cancelled or removed;
this is not reservation-turnover acceptance. The web test author corrected one startup
type by explicitly supplying the fixture's existing harness key.

An initial existing-planning run reported four failures (62 pass) because assertions
still expected the previously unconnected result path. Their deliberate updates retain
all replay/immutability assertions and wrong-run binding refusal, and check the newly
readable exact v2 plan. The browser exclusion check was strengthened. The subsequent
77-test combined planning/submission/owner-review/readback run passed.

Independent fixture and final integrated-evidence reviews found no material false
positive or lost assertion. The shared fixture follows the actual planner, assignment,
fresh signed approval, queue/envelope/transmission/receipt, handoff, registration,
signed progress, ingestion and readback methods; it does not seed a child target directly.
Its local admission stores and profile/transport responses are expressly synthetic.
The same-hash cycle regression is a direct helper probe plus reviewed list composition,
not an end-to-end three-run test. The reviewer did not rerun root's broad suites.

## Verification

- Agent native revised-result suite: 14 passed. Protected/compiled new tests: four passed.
- Root combined new native, existing quality completion and sweep tests: 67 passed.
- Root corrected planning, v1 submission and protected result/review/verification: 77 passed.
- Existing role/submission/owner/result baseline: 70 passed before the association correction;
  later combined and full-lane checks cover the corrected product.
- Both builds and rendered four passed; disposable migrations0001–0054 remain 138 tables.
- Final integrated compiled private suite: 24 passed. New protected-web plus CI inventory:
  10 passed. Final integrated TypeScript and full ESLint: exit zero.
- All six default lanes passed: 2,474 passed, two existing platform skips, zero failures
  or cancellations across 268 lifecycle files. Counts: pre 770; main-1 319; main-2 333;
  main-3 328 plus one skip; main-4 332 plus one skip; post 392.
  The original all-main command exited zero, but main-1's summary was lost at the context
  boundary; that single effect-free lane was rerun and passed 319 in 158.12 seconds.

These are disposable PGlite, authenticated stored evidence, real restricted SQL/HTTP,
compiled in-process startup and fake native transport tests. They do not establish a
live Hermes host, shared live-node recovery, attended browser behavior, physical
PostgreSQL rehearsal or deployed application. No new provider call, credential use,
installation, listener, service, production database or deployment was performed.

PR #333 head `8fa77f9` passed every GitHub job including the aggregate gate in run
`34009139614`. This branch still requires its own current-head CI and dependency-order
integration. No merge is claimed.
Published as [PR #334](https://github.com/MarvinAi5/control-room/pull/334), targeting
the PR #333 branch. Current-head CI remains pending verification.

## Remaining continuous-work integration

The superseded original execution still holds its canonical reservation and is not
marked quality-successful. A bounded, evidence-backed supersession/retirement operation
is needed before ordinary capacity-limited continuous revisions are possible. Do not
raise production capacity, replay native work or invent cancellation/success to hide it.
The browser revision-planning command, native review-plan writer/runtime registration,
background scheduling/recovery and owner signing also remain. Parent workflow/request
completion is a separate pending gate; this block does not adopt draft PR #329.

Continue on Astra Medium with isolated tests and independent higher-effort review.
No owner action is needed for scoped repository integration; live host/database work,
deployment, native qualifications and merges still need their own authority.
