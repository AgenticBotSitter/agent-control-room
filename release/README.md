# Local release metadata candidate

This directory contains declarative planning metadata only. It does not contain an archive, installer, signing material, destination, provider configuration, or executable release command.

`public-package-recipe-v1.json` lists the exact evidence order for a later independently reviewed release process. Every step remains effect-free in this phase.
