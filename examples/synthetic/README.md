# Synthetic Control Room example

This private local candidate evaluates the three fabricated reference adapters entirely in memory. It does not discover an installed harness, open a connection, load local configuration, or perform an operation.

See [Getting started](../../docs/public/GETTING_STARTED.md) for the tested source-workspace flow.
