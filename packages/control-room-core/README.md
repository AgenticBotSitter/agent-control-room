# Control Room public core (local candidate)

This source-only package defines bounded observation contracts. It is a local candidate only: it is not built, signed, installed, or published by this repository block.
