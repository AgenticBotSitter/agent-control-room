# jsonwebtoken

Runtime package jsonwebtoken 9.0.3, maintained at
https://github.com/auth0/node-jsonwebtoken, MIT (Auth0, Inc.).

The adjacent LICENSE is byte-identical to the installed package's original.
The dependency is installed unchanged. Control Room retains its application claim,
encoding, trust, freshness and session rules around the maintained verifier.
Transitive dependencies retain their own licenses; this notice is not complete
clearance for compiled release artifacts.
